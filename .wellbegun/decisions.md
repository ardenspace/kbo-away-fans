# kbo-away-fans — decisions

## L/XL 색인

<!-- L/XL 결정을 원장에 추가할 때 이 색인도 같은 커밋에서 갱신한다. 본문은 아래 원장에. -->

- [XL] 크로스플랫폼 프레임워크는 Flutter (2026-08-24)
- [L] 콘텐츠는 서버 없이 정적 JSON 번들로 배포 (2026-08-24)
- [L] KBO 일정은 크롤링 → schedule.json 계약으로 공급 (2026-08-24)
- [L] 지도는 네이버 지도 SDK + flutter_naver_map (2026-08-24)
- [XL] 계정 모델은 소셜 로그인(구글·카카오·애플) 필수 (2026-09-01)
- [XL] 데이터 소유권은 서버에 계정·닉네임·배지·좋아요까지, 위치는 남기지 않음 (2026-09-01)
- [L] 배지는 "내가 그 구장에 갔는가"를 세는 10칸 판 (2026-09-01)
- [XL] 사용자 데이터 백엔드는 Firebase Auth + Cloud Firestore, 자체 서버 없음 (2026-09-01)
- [L] 소셜 로그인은 구글·애플 기본 제공자 + 카카오 커스텀 토큰(Cloud Functions) (2026-09-01)
- [L] 구장 방문 확인은 포그라운드 위치 판정(경기일·반경·시간 창) (2026-09-01)
- [L] 도장 문서 id 는 `{stadiumId}_{gameId}` 결정적 조합 (2026-09-01)
- [L] 최근 5경기는 점수·승패·구장·날짜까지, schedule 계약 schemaVersion 2 (2026-09-01)
- [L] 배지 판은 사용자 문서의 칸별 요약만 읽고 도장 상세는 칸을 열 때만 (2026-09-01)

## 원장 (시간순 추가 전용)

### 사이클 1 — MVP (2026-08-24 ~ 2026-08-25, 아카이브: `cycles/01/`)

- [2026-08-24] [XL] 크로스플랫폼 프레임워크는 Flutter — 애니메이션 연출이 제품 정체성이고, 콘텐츠 갱신은 원격 JSON이 담당하므로 Expo OTA의 이점이 줄며, 긴급 패치는 Shorebird로 가능; rejected: React Native + Expo (OTA 배포 편의는 강점이나 이 앱에서는 콘텐츠 파이프라인이 그 역할을 대체)
- [2026-08-24] [L] 콘텐츠는 서버 없이 버전 있는 정적 JSON 번들로 배포 — 쓰기 주체가 운영자뿐이고 서버에 개인 데이터를 두지 않는 begin 결정과 정합하며, 운영 부담이 최소; rejected: 백엔드 API 서버 (MVP에 운영·비용 부담만 추가, 개인화 없음이라 이점 없음)
- [2026-08-24] [L] KBO 일정은 스케줄러 크롤링 → schedule.json 계약으로 공급 — 공식 API가 없어 크롤링이 불가피하며, 앱은 JSON 계약만 소비해 소스 교체가 계약 뒤에서 가능; rejected: 앱이 직접 경기 사이트를 크롤링 (차단·파싱 변경에 전체 사용자가 동시 노출)
- [2026-08-24] [L] 지도는 네이버 지도 SDK + flutter_naver_map — 국내 POI·한글 라벨 품질이 핵심이고 사용자가 네이버 계정 보유, Flutter 플러그인이 성숙; rejected: Mapbox (커스텀 모션은 최강이나 국내 장소 데이터 품질 열세), 카카오맵 (Flutter 플러그인 성숙도 부족)
- [2026-08-24] [M] 우천 취소 감지는 일정 크롤러의 경기일 고빈도(15–30분) 실행으로 schedule.json에 반영, 앱은 폴링 — 별도 감지 체계 없이 기존 파이프라인에 얹는 가장 싼 구조
- [2026-08-24] [M] 날씨는 OpenWeatherMap 무료 티어를 앱에서 직접 호출, 얇은 래퍼 뒤에 격리 — 연출·플랜B 유도 용도라 정확도 요구가 낮고 래퍼 뒤 소스 교체가 쉬움
- [2026-08-24] [M] 스탬프 인증 방식은 MVP에서 미결정으로 유지 — 콘텐츠 스키마의 구장 ID를 안정적 식별자로 보장해 도입 문만 열어 둠
- [2026-08-24] [M] UGC 대비는 places 스키마의 source 필드(현재 "curated" 고정) 예약까지만 — 검수·계정 구조는 기능 도입 시점에 결정
- [2026-08-24] [M] 콘텐츠 구장 목록은 1군 정규 홈구장 9곳, 컬러 테마는 팀 10개 — 잠실은 두 팀이 공유하므로 테마는 경기의 홈팀 기준으로 전환하고, 제2구장(포항·울산 등)은 후순위
- [2026-08-24] [M] 성공 지표(추천 탭 → 지도 진입)는 Firebase Analytics 익명 이벤트로 측정 — 계정·개인 데이터 없이 행동 횟수만 집계, "서버에 개인 데이터를 두지 않는다"는 begin 결정과 양립
- [2026-08-24] [M] 상태 관리는 flutter_riverpod — 컴파일 타임 안전한 DI/스코프로 TeamThemeScope류 컨텍스트 주입과 잘 맞고, 루트 ProviderScope만 걸어 두면 도입 비용이 낮음
- [2026-08-24] [S] flutter create는 ios/android 플랫폼만 생성 — 모바일 팬 앱이라 데스크톱/웹 불필요, 나중에 flutter create로 추가 가능
- [2026-08-24] [M] 번들 id org는 dev.arden (dev.arden.kbo_away_fans) — 스토어 배포 전이라 변경 비용이 낮은 시점에 임시 확정
- [2026-08-24] [S] lint 기준선은 flutter_lints + strict-casts/inference/raw-types + 소수 추가 규칙(prefer_single_quotes 등) — analyze 경고 0을 유지 가능한 선에서 엄격하게
- [2026-08-24] [S] 루트 위젯(KboAwayFansApp)을 lib/app.dart로 분리 — smoke test가 main() 부작용 없이 루트 위젯만 펌프할 수 있게
- [2026-08-24] [M] 콘텐츠 계약 검증기는 Node.js + ajv(JSON Schema draft 2020-12), content-pipeline/package.json 로컬 의존성 — 표준 검증기라 스키마와 검증 동작의 드리프트가 없고, 이후 파이프라인 공통 레이어(1.5)도 같은 런타임을 쓸 수 있음
- [2026-08-24] [M] 스키마는 계약 4개 + common.defs(팀 10·구장 9 안정 id enum) 분할 — 팀/구장 id를 한 파일에서 원본 관리해 계약 간 참조 불일치를 스키마 수준에서 차단
- [2026-08-24] [M] 구장 안정 id는 지명 슬러그 9종(jamsil/gocheok/munhak/suwon/daejeon/daegu/sajik/changwon/gwangju), 팀 id는 구단 슬러그 10종 — 구단명 변경에도 안정적인 지명/약칭 기반, 출시 전이라 변경 비용 낮은 시점에 확정
- [2026-08-24] [M] schemaVersion은 각 스키마에서 const 1로 고정 — 버전 불일치가 곧 검증 실패가 되어 별도 버전 비교 코드 없이 계약 위반으로 잡힘; 계약 변경 시 const를 올림
- [2026-08-24] [M] places.category는 enum 5종(food/cafe/escape_room/activity/landmark)으로 시작 — 필터 칩과 1:1 대응을 계약으로 보장, 카테고리 추가는 스키마 수정으로만 가능
- [2026-08-24] [M] 팀 테마 조회 API는 TeamThemes.byId 상수 맵(키 = common.defs teamId 10종) + TeamTheme 값 객체(primary/onPrimary/secondary/onSecondary) — 스키마의 안정 id를 그대로 키로 써서 계약과 코드의 드리프트를 차단
- [2026-08-24] [M] check-hardcoded-values.sh 검출 범위는 hex 색(Color(0x..), #rrggbb)과 치수 리터럴(EdgeInsets/Radius·BorderRadius.circular/SizedBox/fontSize의 숫자), lib/design/·생성 파일(.g/.freezed) 제외 — Duration 리터럴은 치수가 아니고 타임아웃 등 비모션 용도와 구분 불가라 제외
- [2026-08-24] [S] type.fontFamily 토큰은 null(플랫폼 기본)로 시작 — 커스텀 폰트 번들 전에 이름만 박으면 조용히 폴백돼 오히려 해로움, 디자인 단계에서 번들과 함께 교체
- [2026-08-24] [M] StadiumMapView 골격은 flutter_naver_map 의존성 없이 자리 표시 렌더 — SDK 연결(클라이언트 ID·플랫폼 설정)은 지도 step의 일이고, 래퍼 API 뒤라 내부 교체 비용이 낮음
- [2026-08-24] [M] check-registry-sync.sh 는 양방향 검사(폴더 파일→로스터 행, 로스터의 백틱 경로→실제 파일) + 짝 2개(lib/ui/shared ↔ 그 REGISTRY.md, content-pipeline/common ↔ content-pipeline/REGISTRY.md) — acceptance의 "행과 파일 일치"를 그대로 검사하고 참조 스크립트는 한 방향만 봄
- [2026-08-24] [S] TeamThemeScope 는 InheritedWidget + of/maybeOf + forTeam(teamId) 편의 생성자 — 프레임워크 표준 주입 메커니즘, 소비 컴포넌트(DdayHeader/CategoryChip)는 maybeOf 로 스코프 밖 폴백을 가짐
- [2026-08-24] [S] ScratchCard 골격은 탭 즉시 공개(AnimatedContainer 전환) — 긁기 연출(CustomPainter/shader)은 discretion 영역으로 이후 내부만 교체
- [2026-08-24] [M] 파이프라인 단위 테스트는 Node 내장 node:test 러너(`npm --prefix content-pipeline test`) — 의존성 추가 없이 validate.mjs와 같은 런타임, 테스트는 content-pipeline/test/*.test.mjs
- [2026-08-24] [M] fetch 재시도 정책은 기본 재시도 3회·시도당 타임아웃 10s·지수 백오프 500ms 기점, 재시도 대상은 네트워크 오류/타임아웃/429/5xx (그 외 4xx는 즉시 반환) — 크롤 대상 차단을 피하면서 일시 장애만 흡수하는 보수적 기본값, 호출부 옵션으로 조정 가능
- [2026-08-24] [M] 로그 포맷은 JSON Lines({ts,level,script,event,...fields})를 stderr로 — stdout은 산출 데이터용으로 비워 두고, 구조화 필드라 CI 로그 검색이 쉬움
- [2026-08-24] [S] deploy 스텁은 spec 레지스트리 위치(content-pipeline/deploy.mjs)에 두고 "검증 통과 후 배포 대상 미정으로 exit 2" 계약만 고정 — CI가 실수로 호출해도 조용히 성공하지 않게 하고, 호스팅 결정(discretion) 시 내부만 교체
- [2026-08-24] [S] validate.mjs 단위 테스트는 CLI를 자식 프로세스로 실행해 종료 코드를 검증 — 검증자가 실제로 실행하는 계약(exit code) 그대로를 테스트하고 기존 코드를 리팩토링하지 않음
- [2026-08-24] [M] pre-commit wiring은 저장소 내 `scripts/hooks/pre-commit` + `git config core.hooksPath scripts/hooks` (클론 후 1회, README에 명시) — .git/hooks 복사와 달리 훅 본문이 버전 관리되고 설치가 명령 한 줄
- [2026-08-24] [S] check-hardcoded-values 보강: Color.fromARGB/fromRGBO, 간접 hex(0xFF 접두 8자리 리터럴), 3자리 축약 hex(a-f 문자 포함일 때만 — `#123` 이슈 번호 오탐 방지), raw Curves.* 검출 추가; 일반 width/height 숫자 리터럴은 오탐 과다로 의도적 미검출, 검사 범위는 spec대로 lib/ 유지
- [2026-08-24] [S] check-registry-sync 는 레지스트리의 표 행(`|` 시작 줄)만 양방향 매칭 — 산문 속 백틱 언급을 등록으로 오인하던 허점 봉합; REGISTRY.md/CLAUDE.md 메타 파일은 로스터 등록 면제
- [2026-08-24] [S] PostToolUse 명령은 `${CLAUDE_PROJECT_DIR:-.}` + 스크립트 존재 가드로 감싸 스크립트 없는 환경에서도 exit 0 — 세션이 깨지지 않게; PostToolUse 대상은 spec Enforcement plan대로 check-hardcoded-values만(registry-sync는 pre-commit 전용)
- [2026-08-24] [M] 콘텐츠 소비 계층은 `lib/content/`(models·loader·cache·providers, 비 UI 공용) — 여러 feature 가 공유할 위치라 features/ 밖에 둠; 의존성으로 http·path_provider 추가
- [2026-08-24] [M] 팀 테마 조회는 teams.json 의 `themeKey` 를 계약상 포인터로 사용 — 모델 파싱에서 themeKey ∉ TeamThemes.byId 면 문서 전체를 계약 위반으로 거부해, TeamThemeScope.forTeam 의 미지 id null 캐스트 크래시를 로더/모델 계층에서 차단(팀·구장 id enum 도 동일하게 파싱 시 검증)
- [2026-08-24] [M] 로더는 네트워크 우선, 검증(schemaVersion+계약) 통과본만 캐시 갱신 — 실패 사유(network/unsupportedSchemaVersion/contractViolation)를 담아 sealed ContentResult(fresh/fromCache/unavailable)로 반환, "캐시 유지+갱신 거부"가 결과 타입으로 관찰됨
- [2026-08-24] [M] 로컬 캐시는 파일 저장(문서 종류별 1파일, 임시 파일+rename 원자 교체, 만료 없음) — 네트워크 우선이라 캐시는 최후 폴백이며 플러그인 없이 단위 테스트 가능; hive/shared_preferences 불채택
- [2026-08-24] [M] 앱 쪽 계약 검증 범위는 필수 필드·타입·enum·값 범위·개수·교차 id 참조까지, 미지의 추가 키는 무시 — additionalProperties 강제는 파이프라인 validate 소유, 앱은 소비 안전성만 책임
- [2026-08-24] [S] dev 콘텐츠 소스는 로컬 정적 서버(`python3 -m http.server 8787 --directory content-pipeline/data`), URL 상수는 `lib/content/content_config.dart` 의 kContentBaseUrl 하나 — asset 번들 이중 소스를 만들지 않아 "URL 상수 하나로 전환" 계약 유지, 실호스팅 연결은 5.3
- [2026-08-24] [S] 로더 단위 테스트의 정상 픽스처는 `content-pipeline/data/*.json` 실물을 그대로 읽음 — 파이프라인 산출물과 앱 파서 간 드리프트를 테스트가 즉시 잡게
- [2026-08-24] [M] 응원 팀 저장은 shared_preferences 단일 키(`selected_team_id`, 값은 common.defs teamId) — 문서형 콘텐츠 캐시와 달리 스칼라 prefs 라 표준 플러그인이 최소 비용, 미지/오염 값은 읽기 시 null 취급해 온보딩으로 복귀
- [2026-08-24] [M] 온보딩 팀 목록은 teams.json(teamsProvider, 콘텐츠 로더 경유) — 앱 내 하드코딩 로스터 이중화 대신 계약상 포인터(themeKey)를 그대로 소비, 로드 실패 시 명시적 실패 상태 + 재시도(ref.invalidate)
- [2026-08-24] [M] 첫 화면 분기는 루트 게이트 위젯(RootGate: 선택 상태 로딩→스플래시 / 없음·읽기오류→온보딩 / 있음→홈) — 라우터 패키지 없이 위젯 분기 + Navigator.push 로 phase 2–3 범위 충분
- [2026-08-24] [M] 테마 적용은 홈을 TeamThemeScope.forTeam(team.themeKey) 로 감싸 구현, teams 문서를 못 얻은 홈은 스코프 없이 기본 토큰(maybeOf 폴백)으로 렌더 — 미지 themeKey 는 모델 계층에서 이미 거부되므로 스코프는 항상 유효 키만 받음
- [2026-08-24] [S] 팀 변경 진입점은 홈 앱바의 아이콘 버튼 → 같은 TeamSelectScreen 을 변경 모드(isChange)로 push, 선택 즉시 저장 후 pop — 별도 설정 화면은 아직 불필요
- [2026-08-24] [S] 온보딩 팀 목록은 SingleChildScrollView+Column (지연 빌드 없는 전량 렌더) — 10개 고정이라 성능 무의미하고 "10팀 모두 렌더" 검증이 트리에서 직접 가능
- [2026-08-25] [M] 다음 원정 경기 = awayTeamId 가 선택 팀이고 status 가 scheduled 인 경기만 (취소·우천취소는 "갈 경기"가 아니라 제외), 같은 날짜 복수면 시작 시각 빠른 쪽 — 취소 경기로 D-day 를 세우면 빈 상태·다음 경기 안내가 틀어짐
- [2026-08-25] [S] "오늘" 판정은 KST(UTC+9 고정, DST 없음) 달력 날짜 기준, 시작 시각이 지나도 그날 자정까지 "오늘" — schedule.json 의 date 가 KST 계약이고 경기 당일엔 하루 종일 원정 콘텐츠가 유효
- [2026-08-25] [M] "현재 시각"은 clockProvider(Provider<DateTime Function()>, 기본 DateTime.now)로 주입 — D-day 단위·위젯 테스트가 고정 시각으로 결정적이 됨
- [2026-08-25] [M] DdayHeader 빈 상태는 named constructor(DdayHeader.empty)로 확장, 기존 (dDay, matchLabel) 시그니처는 그대로 — 공유 컴포넌트 기존 사용처(테스트 포함) 무파손, REGISTRY 행 설명 동기 갱신
- [2026-08-25] [M] 홈 테마 이중 구조: 앱 골격(앱바)은 응원 팀 테마, D-day 헤더·미리보기 영역만 그 경기 홈팀 테마의 중첩 TeamThemeScope — acceptance "홈팀 기준 구장 테마"를 잠실 2홈팀 케이스 포함 충족하면서 "내 팀" 정체성 유지
- [2026-08-25] [M] schedule/stadiums/places provider 를 teamsProvider 와 같은 패턴(FutureProvider<ContentResult<T>>)으로 추가 — 문서별 독립 실패/캐시 폴백이 화면에서 그대로 관찰됨
- [2026-08-25] [S] schedule 문서를 못 얻은 홈은 로드 중 스피너/실패 안내+재시도(ref.invalidate) — 시즌 종료 빈 상태와 의미가 다르므로 문구를 분리
- [2026-08-25] [S] 원정 미리보기는 목적지 구장의 추천 장소 최대 3곳을 PlaceCard 로, 장소가 없으면 준비 중 문구 — 개수는 discretion 명시 항목
- [2026-08-25] [S] team_select 위젯 테스트는 홈이 소비하는 콘텐츠 provider 4종을 모두 override (schedule 은 빈 일정 고정) — 실제 파일/네트워크 IO 는 widget test fake async 에서 완료되지 않아 pumpAndSettle 이 멈추는 것을 차단하고 현재 시각 의존 제거
- [2026-08-25] [M] category enum ↔ 한국어 문구 매핑은 `lib/ui/shared/category_labels.dart` 의 kCategoryLabels 단일 맵(+ categoryLabelOf, 누락 시 assert) — 칩·카드·시트·미리보기가 전부 이 맵을 소비해 문구 사본 분산을 차단, REGISTRY 행 동시 추가
- [2026-08-25] [M] 추천 필터는 순수 함수 filterPlaces(stadiumId 필수, category?·indoorOnly?) 하나로 — 화면과 분리돼 구장/카테고리+실내/빈 결과가 위젯 없이 단위 테스트되고, 미리보기 등 다른 소비처도 같은 규칙 사용 가능
- [2026-08-25] [S] 추천 목록 정렬은 places.json 문서 순서(큐레이션 순서) 그대로 — 거리·평점 데이터가 아직 없어 임의 정렬을 만들지 않음 (discretion 명시 항목)
- [2026-08-25] [S] 실내 필터는 카테고리 칩 행 끝의 '실내만' 토글 칩으로, CategoryChip 재사용 — 별도 스위치 UI 없이 한 줄에서 조합 필터 완결
- [2026-08-25] [S] 추천 목록 진입점은 홈 원정 미리보기 헤더의 '전체 보기' 버튼 → StadiumPlacesScreen(stadiumId, 그 경기 홈팀 themeKey) push — 미리보기와 같은 구장 맥락·홈팀 테마가 이어짐
- [2026-08-25] [S] "다시 시도"는 invalidateContent(ref) 단일 경로로 콘텐츠 provider 4종을 함께 무효화 — scheduleProvider 만 invalidate 하면 부분 복구 시 teams/stadiums/places 가 실패값으로 남아 홈이 raw id 로 저하 렌더되는 비일관성(실측)을 바로잡음; 홈·추천 목록 양쪽이 같은 경로 사용
- [2026-08-25] [M] ScratchCard 긁기 구현은 CustomPainter — saveLayer+BlendMode.clear 스트로크로 커버를 지우고, 10×4 그리드 커버리지 비율 ≥ 0.55 에서 공개(커버 페이드 MotionTokens.base/standard, 프롬프트 페이드 fast/standard) — shader 불채택: 의존성 없이 위젯 테스트로 검증 가능하고 discretion 범위(구현 방식) 내
- [2026-08-25] [M] ScratchCard props 는 하위 호환 확장: hiddenLabel·onRevealed 시그니처 유지 + hiddenSublabel·onRescratch 추가 — 재긁기 탭 시 카드가 커버·긁기 상태를 스스로 리셋한 뒤 onRescratch 로 소유자가 숨김 내용을 갈아끼움(범용 동작은 공유 컴포넌트, 내용 선택은 소유자 경계), REGISTRY 행 설명 동기 갱신
- [2026-08-25] [M] 랜덤 장소 선택은 순수 함수 pickScratchPlace(pool, Random 주입) — 화면은 pick 이 현재 필터 풀을 벗어날 때만 재선택하고 그때 라운드 키(ValueKey)로 커버를 리셋, 풀이 비면 목록 항목 자체를 만들지 않아 카드 비노출이 트리에서 관찰됨
- [2026-08-25] [S] 재긁기 풀은 현재 필터 풀 그대로(직전 장소 재등장 허용 — acceptance 는 "다를 수 있다"만 요구), 브러시 반경은 SpaceTokens.xl 재사용, 그리드 밀도·임계 비율은 연출 수치가 아닌 판정 파라미터로 위젯 내 상수
- [2026-08-25] [M] 네이버 클라우드 클라이언트 ID 는 `--dart-define=NAVER_MAP_CLIENT_ID` 주입, main 의 StadiumMapView.ensureInitialized 가 읽어 SDK 초기화 — 미주입·초기화 실패면 조용히 건너뛰고 모든 지도가 자리 표시 폴백으로 렌더돼 키 없이 빌드·analyze·테스트가 통과(실 키 연결·실기기 확인은 5.3)
- [2026-08-25] [M] StadiumMapView 실 SDK 연결 props 는 중심 좌표+`StadiumMapMarker` 값 객체 목록(+initialZoom) — SDK 타입(NMarker/NLatLng)을 래퍼 밖으로 새지 않게 하는 경계, 기존 stadiumId props 사용처는 자기 위젯 테스트뿐이라 변경 비용 낮음
- [2026-08-25] [M] 길안내는 nmap://route/public(대중교통)+appname 딥링크 우선, 실패 시 map.naver.com/p/directions 웹 폴백; URL 빌더·공유 페이로드는 SDK 무관 `lib/ui/shared/map_links.dart` 순수 함수로 분리해 좌표·이름 인코딩을 단위 테스트 — launcher 는 주입 가능(UriLauncher)해 폴백 순서도 테스트됨
- [2026-08-25] [M] 공유 페이로드는 "[카테고리] 이름 + 네이버지도 검색 링크(map.naver.com/p/search/이름)" 텍스트를 share_plus ShareParams.text 하나로 — 좌표 없는 이름 검색 링크가 웹에서 가장 안정적으로 열림
- [2026-08-25] [S] 지도 진입은 PlaceMapScreen(장소+구장 마커, 중심은 두 좌표의 중점, 기본 줌 14, 하단 길안내 버튼) push, 시트 액션은 지도/길안내/공유 3버튼 + 주소·소개 표시 — 시트 레이아웃·줌 수치는 discretion 명시 항목
- [2026-08-25] [S] nmap 스킴 가시성 등록 — AndroidManifest `<queries>`(nmap·https)와 iOS LSApplicationQueriesSchemes(nmap), url_launcher 가 앱 미설치를 판별할 수 있게
- [2026-08-25] [M] 분석 래퍼는 `lib/analytics/analytics.dart` 단일 파일 — AnalyticsClient 베이스(단일 검증 경로 `log` + 추상 `send`) + FirebaseAnalyticsClient + analyticsProvider(Provider 주입, 테스트는 기록용 fake 로 override); firebase_core/firebase_analytics import 는 이 파일 안에만 (lib/content 처럼 비 UI 공용 계층이라 lib/ui/shared 로스터 대상 아님)
- [2026-08-25] [M] 분석 화이트리스트는 이벤트 이름 {place_tap, map_open}·파라미터 키 {stadium_id, category} 상수 셋 — 벗어나면 래퍼가 ArgumentError 로 전송 자체를 차단해 "개인 식별 정보 없음"이 코드 경로로 강제됨 (진단 이벤트 추가는 로스터 수정으로만 가능)
- [2026-08-25] [M] Firebase 미초기화 no-op 방식: main 의 ensureInitialized 가 Firebase.initializeApp 을 try/catch — 설정 파일(google-services.json / GoogleService-Info.plist) 없음·초기화 실패 시 백엔드 null 로 남아 이벤트를 조용히 버리고, 전송은 fire-and-forget(catchError 흡수)이라 실패가 UI 흐름을 막지 않음 — 설정 없이 빌드·analyze·테스트 전부 통과
- [2026-08-25] [S] 이벤트 발생 지점은 추천 목록 화면의 전이 코드 한 곳씩 — place_tap 은 카드 탭(_showDetailSheet 진입), map_open 은 지도 화면 push 전이(_openMap) — 성공 지표 "추천 탭 → 지도 진입"의 두 끝점과 1:1
- [2026-08-25] [S] analysis_options 에 `build/**` exclude 추가 — iOS SPM 이 firebase 의존성 소스(example 포함)를 build/ios/SourcePackages 로 체크아웃하면서 analyze 에 끌려 들어와 경고 0 기준선이 깨지는 것을 차단 (빌드 산출물은 lint 대상 아님)
- [2026-08-25] [M] 날씨 래퍼는 `lib/weather/weather.dart` 단일 파일 — WeatherEffect{none,rain} + WeatherService.effectAt + weatherEffectProvider(FutureProvider.family, 좌표 record 키); OpenWeatherMap 의존(호스트·경로·파싱·키)은 이 파일 안에만 두고, 모든 실패(키 미주입·네트워크·비 200·파싱)는 WeatherEffect.none("연출 없음")으로 흡수해 예외를 전파하지 않음 — analytics 처럼 비 UI 공용 계층이라 lib/ui/shared 로스터 대상 아님
- [2026-08-25] [M] OpenWeatherMap API 키는 `--dart-define=OPENWEATHER_API_KEY` 주입 (NAVER_MAP_CLIENT_ID 와 같은 패턴) — 하드코딩 금지, 미주입이면 호출 없이 연출 없음이라 키 없이 빌드·analyze·테스트 전부 통과
- [2026-08-25] [M] 날씨 갱신 주기는 좌표별 앱 세션 캐시(provider family 캐시, 타이머·폴링 없음) — 연출 용도라 실시간성 요구가 낮고 무료 티어 호출량을 아끼며, 강제 갱신은 invalidate 한 줄
- [2026-08-25] [M] 비 연출은 RainLayer(repeat AnimationController + CustomPainter, 고정 시드 의사난수 빗줄기) — 연출 수치는 토큰으로(RainTokens 개수·길이·굵기·기울기 + MotionTokens.rainFall + ColorTokens.rainDrop), 눈 등 추가 날씨 연출은 미도입(discretion); 비 상태에서만 레이어가 트리에 존재해 위젯 테스트가 find.byType 으로 관찰
- [2026-08-25] [S] 날씨 기준 좌표는 홈=다음 원정 경기 목적지 구장, 추천 목록=그 화면 구장 — WeatherBackdrop 은 raining bool 만 받아 날씨 계층과 비결합 유지(테스트 주입 경계)
- [2026-08-25] [M] 오늘 취소 감지는 sealed NextAwayGame 확장 대신 별도 순수 함수 findTodayCanceledAwayGame(→ Game?, non-null = 플랜B 모드) — 기존 exhaustive switch 사용처 무파손, 다음 경기 계산과 독립이라 취소일에도 D-day 얼굴이 그대로 동작
- [2026-08-25] [M] StadiumPlacesScreen 에 initialIndoorOnly(기본 false) 생성자 파라미터 추가 — 기존 사용처·테스트 무파손 하위 호환 확장, 실내 필터(_indoorOnly) 초기값만 주입
- [2026-08-25] [S] 플랜B 배너는 홈 전용(홈 파일 내 렌더, 공유 로스터 미등록 — 렌더 지점 1곳) — 상태별 문구(우천/일반 취소, warning 토큰 액센트) + '실내 놀거리 보러 가기' 버튼이 취소 경기 구장의 추천 목록을 실내 필터 켠 채·홈팀 테마로 push ('전체 보기'와 같은 테마 규칙)
- [2026-08-25] [S] 홈 비 연출 좌표 우선순위: 오늘 취소된 경기 구장 > 다음 원정 목적지 — 우천 취소일에도 4.1 비 연출과 플랜B 유도가 함께 동작
- [2026-08-25] [S] 탐색 진입점은 홈 ListView 하단 상시 노출 섹션(StadiumPicker 재사용, stadiums 문서 미획득 시 비노출) — "경기 없는 날의 두 번째 진입점"의 상위집합이라 어떤 얼굴 상태에서도 접근 가능, discretion 명시 항목(진입점 위치)
- [2026-08-25] [S] 구장 목록 표현은 StadiumPicker 골격의 칩 Wrap 그대로, 라벨은 stadium.name — 지도/그리드 불채택(9개 고정이라 칩 나열로 충분, 지도는 SDK 키 의존), discretion 명시 항목
- [2026-08-25] [M] 잠실 탐색 테마 규칙은 순수 함수 browseThemeKeyForStadium(lib/features/home/stadium_browse.dart) — 당일(KST) 잠실 경기의 홈팀 테마(취소·우천취소 경기도 "그날의 홈팀"으로 인정해 플랜B 테마 규칙과 일관, 복수면 시작 시각 빠른 쪽), 당일 경기 없음·schedule 미획득이면 중립(null = 팀 스코프 없음); 단일 홈팀 구장은 경기 유무 무관 그 팀 테마
- [2026-08-25] [S] 탐색 화면은 신설 없이 StadiumPlacesScreen(stadiumId, themeKey) 재사용 — 테마 전환·추천 목록·뒤로 가기(pop)가 기존 계약 그대로 동작, "뒤로 가면 내 팀 홈"은 push/pop 으로 자동 충족
- [2026-08-25] [M] 일정 크롤 소스는 네이버 스포츠 일정 API(api-gw.sports.naver.com/schedule/games, JSON) — KBO 공식(koreabaseball.com)은 robots.txt 가 전 경로 크롤을 금지("사전 승인 없이 자동 수집 금지" 명시)하고 다음 스포츠 API 경로(/prx/)도 robots Disallow 라 제외; 네이버는 robots 제약 없음(404)·구조화 JSON 이라 파싱 안정, 소스 교체는 crawl-schedule.mjs 한 파일의 파서 교체로 국소적
- [2026-08-25] [M] 상태 매핑은 cancel=true & statusInfo 에 '우천' 포함 → rain_canceled, 그 외 취소 → canceled, 나머지(경기전·진행·종료·서스펜디드) → scheduled — 네이버는 지난 취소를 '경기취소'로 정규화해 우천 구분이 당일 라벨에만 있으므로, 이전 산출물이 rain_canceled 인 경기는 정규화된 취소 라벨로 강등하지 않고 유지(경기일 고빈도 크롤이 당일 라벨을 포착하는 전제)
- [2026-08-25] [M] 게임 id 는 네이버 gameId 원문 사용, 로스터 밖 팀 코드·구장 표기(제2구장 포항·울산 등) 경기는 warn 로그 후 제외 — id 는 유일·안정하고 앱은 opaque 문자열로 소비, 9구장 로스터 밖 경기는 앱 콘텐츠가 없어 무의미
- [2026-08-25] [M] 크롤 창은 KST 오늘부터 30일 단일 요청, games 내용이 기존 산출물과 같으면 파일 미갱신 — 오늘 취소 감지·D-day 계산에 충분하고 과거 경기는 자연 탈락, generatedAt 만 바뀌는 고빈도 cron 커밋 노이즈를 크롤러 수준에서 차단 (superseded 2026-09-01)
- [2026-08-25] [M] 크롤 산출 교체는 임시 파일(schedule.next.json)에 쓰고 validate.mjs 통과 시에만 rename — fetch/파싱/검증 어느 실패든 기존 schedule.json 무변경 + 실패 로그 + exit 1
- [2026-08-25] [M] schedule game id 유일성은 validate.mjs 의 스키마 후 의미 검사(SEMANTIC_CHECKS)로 강제 — JSON Schema 로 표현 불가한 규칙이라, 크롤러뿐 아니라 수작업 편집·deploy 등 모든 검증 경로가 같은 게이트를 지나 앱 파서(중복 id 문서 거부)와의 드리프트를 봉합
- [2026-08-25] [M] cron 은 저빈도 매일 21:30 UTC(06:30 KST) + 고빈도 05:00–14:40 UTC 20분 간격(14:00–23:40 KST) — 주말 낮 경기(14:00 KST)부터 야간 경기·취소 공지까지 커버, 변경 시에만 커밋이라 고빈도 부담 없음
- [2026-08-25] [S] 시범 구장은 잠실 — 두 홈팀(LG·두산) 공유라 원정 수요가 가장 크고, 기존 테스트·픽스처가 잠실 기준이라 실데이터 전환 비용이 최소
- [2026-08-25] [M] 장소 큐레이션 워크플로는 `curation/<stadiumId>.json` 입력 + `build-places.mjs` 병합기 — 파일명↔stadiumId↔장소 stadiumId 3중 정합 검사 후 임시 파일(places.next.json)에 쓰고 validate 통과 시에만 rename (crawl-schedule 과 같은 산출물 보호 패턴); data/places.json 은 산출물이고 원본은 curation/ 입력, 나머지 8구장은 CURATION.md 반복
- [2026-08-25] [M] validate.mjs 의미 검사를 4개 문서 전부의 id 유일성으로 확장 (duplicateIds 공통 헬퍼) — 앱 파서는 teams/stadiums/places/schedule 모두 id 중복 문서를 거부하는데 파이프라인은 schedule 만 잡던 드리프트 봉합 (이전 검증 지적 사항); 실패 픽스처 3종 추가로 픽스처 열거 테스트가 자동 커버
- [2026-08-25] [S] 플랜B 커버리지 임계(장소 10·카테고리 3·실내 3)는 병합기 warn + CURATION.md 문서화 — 채우는 중인 구장이 있어 빌드 실패로는 안 만들고, 출시 전 전 구장 충족을 운영 조건으로 명시; 시범 구장 충족은 파이프라인 테스트와 앱 통합 테스트가 단언
- [2026-08-25] [M] 대전 한화생명 볼파크 좌표를 36.3163/127.4314 로 교정 (위키백과 좌표, 중구 부사동 대종로 373) — 기존 샘플 값(36.3403/127.3869)은 둔산동 방향 약 5km 오차; 나머지 8구장 좌표·명칭은 실제 값과 일치 확인
- [2026-08-25] [S] 샤라웃은 매체 근거가 확인된 4곳만 (에스콰이어 '선수 Pick' 기사 등) — 기사에 함께 언급된 돈사돈(장원준 단골)은 잠실 인근 지점 실재를 확인 못 해 제외 (허위 출처 금지 규칙); 나머지는 shoutout 없는 일반 큐레이션
- [2026-08-25] [M] 콘텐츠 호스팅은 GitHub Pages, 배포는 deploy.mjs 가 git plumbing(hash-object→mktree→commit-tree)으로 4종 JSON+.nojekyll 고아 커밋을 gh-pages 브랜치에 force-push — 이력 원본은 main 의 data/ 커밋이라 gh-pages 이력 불필요, 원격 트리 동일 시 push 생략(idempotent); Actions 공식 Pages 배포(actions/deploy-pages) 대신 브랜치 방식이라 로컬·CI 어디서든 스크립트 한 번으로 배포 가능
- [2026-08-25] [M] 크롤 cron → 배포 연결은 crawl-schedule.yml 안에서 deploy.mjs 직접 실행(변경 커밋 또는 수동 실행 시) — GITHUB_TOKEN push 는 다른 워크플로의 on: push 를 트리거하지 않아 별도 워크플로로는 안 이어짐; 사람 push 경로는 deploy-content.yml(on: push paths=data/·schema/·common/·deploy.mjs)이 커버
- [2026-08-25] [M] kContentBaseUrl 은 String.fromEnvironment('CONTENT_BASE_URL') 기본값 https://ardenspace.github.io/kbo-away-fans — 기존 dart-define 패턴(NAVER_MAP_CLIENT_ID·OPENWEATHER_API_KEY)과 동일하게 로컬/CI 는 빌드 시 오버라이드, "URL 상수 하나로 전환" 계약 유지
- [2026-08-25] [S] 로더 HTTP 타임아웃은 kContentFetchTimeout 10초 (Future.timeout, TimeoutException 은 기존 network issue 분류로 흡수) — 파이프라인 fetch 공통의 시도당 타임아웃과 같은 값, 실기기 첫 로드 무기한 대기 차단
- [2026-08-25] [S] 실 URL 통합 테스트는 test/content/content_loader_live_test.dart 로 두고 CONTENT_LIVE_TEST dart-define 게이트로 skip 기본 — 평상시 flutter test 를 네트워크 없이 유지, CI/로컬은 CONTENT_BASE_URL 오버라이드로 스테이징·로컬 서버 대상 실행 가능 (integration_test/ 는 실기기 필요라 불채택)
- [2026-08-25] [S] teams.schema.json themeKey 를 slug → common.defs teamId enum 참조로 제한 (validate ↔ 앱 파서 드리프트 봉합) — schemaVersion 은 1 유지: 값 공간이 앱 파서(TeamThemes.byId)가 이미 요구하던 부분집합으로 좁아질 뿐, 기존 유효 데이터·앱 하위 호환 모두 무변경이라 마이그레이션급 아님
- [2026-08-25] [M] 세션 중 우천 취소 반영은 앱 루트(KboAwayFansApp → ConsumerStatefulWidget + WidgetsBindingObserver)에서 resumed 전이 시에만 invalidateContent 호출 — 타이머 폴링 불채택(백그라운드→포그라운드 전이 이벤트만이라 과도 재조회 없음, 크롤 cron 주기 대비 충분), 위젯 테스트는 handleAppLifecycleStateChanged 로 전이를 통지해 재로드를 단언
- [2026-08-25] [M] Firebase Android 활성화는 조건부 plugin wiring 채택: settings.gradle.kts 에 com.google.gms.google-services 4.5.0 apply false 등록 + app 모듈이 google-services.json 존재 시에만 apply — json 없는 클론에서도 빌드 무파손이 문서화(b안)보다 확실히 강제됨; 4.5.0 은 AGP 9.0.1 대응 최신(4.4.x 는 AGP 8 세대), 이 머신에 Android SDK 가 없어 Gradle 빌드 검증은 불가라 아티팩트 존재를 Google maven metadata 로 확인
- [2026-08-25] [S] 로컬 콘텐츠 서버 예시 포트를 8787 → 8899 로 변경 (README·content_config.dart·live test 주석) — 8787 은 개발 머신에서 타 프로젝트가 점유 중이라 예시 그대로 복붙하면 충돌; decisions.md 의 과거 결정 문구는 이력이라 그대로 둠
- [2026-08-26] [M] 앱 식별자 org 를 dev.arden → com.ardenspace 로 통일 (Android applicationId·namespace·MainActivity 패키지, iOS PRODUCT_BUNDLE_IDENTIFIER 6곳, map_links.dart 의 kNaverMapCallerAppName) — NCP Maps Application 에 이미 등록된 값이 com.ardenspace 계열이었고 스토어 배포 전이라 변경 비용이 여전히 낮은 시점, 2026-08-24 의 "dev.arden 임시 확정"을 대체. 대가로 Firebase Android/iOS 앱 재등록(설정 파일 재발급)이 필요하다

### 사이클 2 — 계정·배지·개인화 (2026-09-01 개시)

<!-- 이 사이클의 결정을 여기에 시간순으로 추가한다. L/XL 은 위 색인도 같은 커밋에서 갱신. -->

- [2026-09-01] [XL] 계정 모델은 소셜 로그인(구글·카카오·애플) 필수 — 앱 전체가 계정을 요구하고 첫 화면이 로그인; 배지는 몇 년에 걸쳐 모으는 데이터라 기기 유실이 곧 수집의 전제를 무너뜨리고, 아직 스토어 미출시라 로그인 벽에 막힐 기존 사용자가 없어 지금이 가장 싼 시점, 애플은 제3자 소셜 로그인 제공 시 App Store 심사 지침이 요구하므로 선택 아님; supersedes: [사이클 1 begin] 계정 없이 시작(팀 선택은 기기에만 저장); rejected: 이메일·비밀번호 직접 가입 (비밀번호 재설정·이메일 인증·도용 대응을 전부 만들어야 하고 "없는 이메일로 가입" 문제가 생김), 네이버 추가 (지도가 네이버인 것과 무관 — nmap 딥링크는 로그인과 연결되지 않고, 제공자가 늘수록 같은 사람이 다른 제공자로 재가입해 배지가 갈라질 위험만 커짐), 배지만 계정 필수 (경로가 둘로 갈려 저장소를 이중으로 만들어야 함)
- [2026-09-01] [XL] 데이터 소유권은 서버에 계정 식별자·닉네임·배지 기록(구장+날짜)·좋아요 목록까지 두되 위치 좌표와 그 이력은 보내지도 저장하지도 않음 — 구장 근처 판정은 기기에서 하고 결과만 올려 개인 위치 이력 보관을 피함; supersedes: [사이클 1 begin] 서버에 개인 데이터를 두지 않는다; rejected: 위치 이력 저장 (부정 획득 검증과 "내가 다닌 곳" 확장이 가능해지지만 개인 위치 이력 보관이라는 무게가 이득보다 큼)
- [2026-09-01] [XL] 사용자 데이터 백엔드는 Firebase Authentication + Cloud Firestore, 자체 운영 서버 없음 — 앱에 firebase_core/firebase_analytics 가 이미 들어와 있어 두 번째 백엔드를 얹지 않아도 되고, Firestore 모바일 SDK 의 오프라인 지속성이 "구장에서 통신이 나빠도 도장은 그 자리에서 찍힌다"를 기본 동작으로 흡수하며, 무료 플랜에 비활성 정지 정책이 없음; 콘텐츠(팀·구장·장소·일정)는 기존 정적 JSON 파이프라인 그대로 두고 Firebase 는 사용자 데이터만 담당; rejected: Supabase (카카오가 내장 소셜 제공자라는 큰 이점이 있으나, 무료 플랜이 7일 비활성 시 프로젝트를 정지시켜 출시 전·초기 구간에 실제로 걸리고, 오프라인 쓰기 큐를 직접 만들어야 하며, 카카오 제공자가 account_email 필수 동의를 요구해 비즈니스 채널 등록 없이는 막힌다는 보고가 있음), 자체 백엔드 API 서버 (1인 운영에 배포·모니터링·보안 부담이 그대로 남고 사이클 1이 이미 같은 이유로 기각)
- [2026-09-01] [L] 소셜 로그인은 구글·애플을 Firebase Auth 기본 제공자로, 카카오는 Cloud Functions(Firebase Admin SDK)가 카카오 액세스 토큰을 검증해 커스텀 토큰을 발급하는 방식 — 커스텀 토큰으로 만든 계정은 일반 MAU 로 계산돼 5만 명까지 무료이고, 카카오 동의 항목을 우리가 통제해 이메일 스코프(비즈니스 채널 필요) 없이 uid·닉네임만 받을 수 있음; rejected: Firebase Authentication with Identity Platform 의 OIDC 커스텀 제공자로 카카오 등록 (카카오가 OIDC 를 opt-in 지원해 서버 코드가 아예 없어지고 토큰 검증을 직접 구현하지 않아도 되지만, OIDC 제공자는 무료 한도가 50 MAU 뿐이고 이후 MAU 당 $0.015 가 붙어 무료 앱의 주력 로그인 수단으로는 비용 구조가 맞지 않음)
- [2026-09-01] [L] 구장 방문 확인은 앱이 열려 있을 때만 하는 포그라운드 판정 — (1) 그 구장에 그날 경기가 있고 (2) 기기 위치가 구장 좌표 반경 안이며 (3) 경기 시각 기준 시간 창 안일 때 도장; 백그라운드 위치를 쓰지 않으므로 사용자가 구장에서 앱을 열어야 하는데, 그 행동이 오히려 begin 이 "가장 중요한 연출"로 지목한 도장 획득 순간을 만든다; rejected: 백그라운드 위치·지오펜싱 (앱을 열지 않아도 자동으로 찍히지만 iOS "항상 허용" 권한의 거부율과 심사 부담, 상시 위치 추적의 배터리 비용을 지면서 정작 도장이 찍히는 순간의 연출을 잃음), 자가 신고 (begin 에서 non-goal 로 확정)
- [2026-09-01] [L] 도장 문서 id 는 `{stadiumId}_{gameId}` 결정적 조합 — 오프라인 재시도·중복 탭·여러 기기 동시 접속에서 같은 경기의 도장이 같은 문서로 수렴해 중복 방지를 위한 별도 코드가 필요 없고, Firestore 오프라인 쓰기가 그대로 멱등해짐; rejected: 자동 생성 id + 서버측 중복 검사 (쓰기마다 조회가 필요해 오프라인에서 판정할 수 없고, 오프라인 큐가 쌓였다 한꺼번에 올라갈 때 중복이 그대로 통과)
- [2026-09-01] [L] 최근 5경기 결과는 점수·승패·구장·날짜까지만 담고 선발 투수와 날씨는 이번 사이클에서 제외 — 네이버 스포츠 일정 API 응답에 homeTeamScore/awayTeamScore/winner 가 이미 있어 크롤 창을 과거로 넓히고 schedule 계약의 schemaVersion 을 2로 올리는 것으로 끝나며, 선발 투수는 경기 상세를 경기마다 따로 긁는 크롤 단계가 하나 더 필요하고 지난 경기의 날씨는 OpenWeatherMap 무료 티어에 과거 데이터가 없어 원천 자체가 없음; rejected: 노션 문서대로 선발 투수·날씨까지 전부 (팬에게는 의미 있는 정보지만 크롤 실패 지점이 늘고, 날씨는 지금부터 경기일마다 기록해 두는 구조를 새로 만들어도 기록 이전 경기가 영구히 빈칸으로 남아 화면이 반쪽이 됨)
- [2026-09-01] [L] 배지 판은 사용자 문서에 든 칸별 요약(구장별 도장 개수·등급)만 읽어 그리고, 개별 도장 문서는 칸 상세를 열 때만 읽음 — 도장 쓰기가 요약 갱신을 함께 수행(같은 트랜잭션); 판을 열 때마다 도장을 전부 읽으면 읽기 수가 사용자 수 × 도장 수로 늘어 오래 쓴 사람일수록 비싸지고 무료 할당량(일 5만 읽기) 기준 약 800명에서 한도에 닿는 반면, 요약 방식은 읽기가 사용자 수에만 비례해 약 2만 5천명까지 여유가 생김; rejected: 판을 열 때마다 도장 문서를 전부 읽어 집계 (쓰기가 단순하고 요약이 실제 도장과 어긋날 위험이 없지만, 인기가 늘수록 나빠지는 구조라 성공이 곧 비용 사고가 됨)
- [2026-09-01] [M] 운영 비용 제약: Firebase 는 Blaze(종량제)로 전환하되 예산 알림과 상한을 먼저 걸고, Firestore 읽기 패턴은 무료 할당량(일 5만 읽기·2만 쓰기) 안에 머무는 것을 설계 조건으로 둠 — Cloud Functions 가 Spark 플랜에서 배포되지 않아 카드 등록은 피할 수 없지만 무료 할당량은 Blaze 에도 그대로 포함되고, 배지 판처럼 자주 여는 화면은 Firestore 오프라인 캐시를 우선 읽어 원격 읽기를 줄인다; 무료로 유지되는 것: Firebase Auth(5만 MAU), 카카오 개발자, 구글 로그인, 네이버 스포츠 API, GitHub Pages
- [2026-09-01] [S] 애플 로그인 설정에는 Apple Developer Program(연 $99)이 필요할 가능성이 높음 — 2차 개발이 만든 비용이 아니라 App Store 출시에 어차피 필요한 지출이며 시점만 앞당겨짐, 착수 전 무료 계정으로 Sign in with Apple capability 설정이 가능한지 실제로 확인할 것
- [2026-09-01] [M] 위치 권한은 온보딩의 팀 선택 직후에 용도(구장 도장·현재 위치 표시)를 설명하고 요청, 거절해도 앱의 나머지는 그대로 동작 — 배지 탭에서 처음 묻는 방식보다 홈 상단 현재 위치 표시가 첫 진입부터 필요하다는 점에 맞고, 노션의 "첫 진입 팝업"보다는 팀 선택을 마쳐 맥락이 생긴 뒤가 수락률이 높음
- [2026-09-01] [M] 하단 5탭은 라우터 패키지 없이 BottomNavigationBar + IndexedStack + 탭별 Navigator 로 구성, 로그인 게이트는 기존 RootGate 를 확장 — 탭마다 독립 스택을 유지해 탭을 오갔다 돌아와도 보던 화면이 남고, 사이클 1의 "라우터 패키지 없이 위젯 분기 + Navigator.push" 판단을 탭 구조까지 그대로 연장
- [2026-09-01] [M] 선택 팀의 원본은 Firestore 사용자 문서로 옮기고 shared_preferences 는 오프라인 첫 렌더용 캐시로만 남김 — 계정이 필수가 되면서 기기 저장이 원본일 이유가 없어졌고, 캐시를 없애면 로그인 직후 첫 프레임에 팀 테마가 늦게 붙음
- [2026-09-01] [L] 배지는 홈·원정 구분 없이 "내가 그 구장에 갔는가"를 세고, 판은 팀 테마 10개와 1:1 대응하는 10칸(잠실은 그날 홈팀 기준 두 칸) — 빈 칸까지 처음부터 보여주기로 한 이상 야구 용어의 원정으로 세면 내 팀 홈구장 칸이 구조적으로 영원히 빈칸이 되어 완주라는 목표 자체가 성립하지 않음(서울 사는 엔씨 팬의 창원, 두산 팬의 잠실-두산); rejected: 원정 경기만 카운트 (앱 정체성에는 맞지만 채울 수 없는 칸이 판에 박힘), 팬마다 판 크기를 다르게 (설명이 복잡하고 완주 비교가 불가능)

- [2026-09-01] [M] 앱의 지원 schemaVersion 을 단일 상수(kSupportedSchemaVersion)에서 문서별 상수 4개(kTeamsSchemaVersion·kStadiumsSchemaVersion·kPlacesSchemaVersion·kScheduleSchemaVersion)로 쪼개고 ContentKind 가 자기 버전을 들게 함 — schedule 만 2로 올라가는데 상수가 하나면 나머지 3종(여전히 1)이 전부 미지원으로 거부되어 콘텐츠가 통째로 멈춘다; 계약이 문서마다 따로 진화하는 이상 버전도 문서마다 따로여야 함; rejected: 4종을 한꺼번에 2로 올리기 (내용이 안 바뀐 계약의 버전을 올리는 것이라 "버전이 곧 계약 변경"이라는 규칙이 무의미해지고, 앞으로 한 문서를 고칠 때마다 4개 산출물을 전부 재배포해야 함)
- [2026-09-01] [S] schedule 계약의 승패는 점수에서 파생하지 않고 result 필드(home_win/away_win/draw)로 명시하되, 점수와의 정합은 validate.mjs 의미 검사와 앱 파서가 같은 규칙으로 강제 — KBO 는 무승부가 있어 소비하는 쪽마다 "동점이면 draw" 규칙을 다시 쓰게 되고, 이중 표현의 유일한 위험인 어긋남은 양쪽 게이트가 막는다(크롤러도 winner 필드를 받지 않고 점수에서 계산해 게이트를 항상 통과); 끝나지 않은 경기에 점수가 붙는 것은 계약이 금지 — 중간 점수를 최종처럼 보여주는 사고를 스키마 수준에서 차단
- [2026-09-01] [M] 오늘 열린 원정 경기는 finished 로 바뀐 뒤에도 그날 자정까지 홈의 "오늘 경기"로 남는다 — status 에 finished 가 생기기 전부터 next_away_game.dart 가 문서로 못 박아 온 불변식("오늘 경기는 시작 시각이 지났어도 그날 자정까지 오늘로 본다")을 새 상태값이 깨지 않게 하려는 것이고, 원정을 다녀온 팬이 경기 후 밤에 앱을 열었을 때 오늘의 원정 맥락이 남아 있어야 이 사이클에서 붙는 도장·좋아요와 앞뒤가 맞는다; 취소·우천취소는 종전대로 "갈 경기"가 아니므로 계속 건너뛴다
- [2026-09-01] [S] 같은 날 원정 경기가 둘일 때(더블헤더) 홈이 고르는 "오늘 경기"는 종료 여부와 무관하게 시작 시각이 빠른 쪽 — next_away_game.dart 가 원래부터 문서로 못 박은 정렬 규칙(날짜 → 시작 시각)을 finished 가 후보에 들어온 뒤에도 그대로 유지한다; rejected: 같은 날짜에서 scheduled 를 finished 보다 우선 (아직 남은 경기를 보여주는 편이 자연스러워 보이지만 KBO 더블헤더는 드물고, 상태별 우선순위를 정렬에 섞으면 "날짜 → 시작 시각" 한 줄 규칙이 무너져 되돌리기 비용만 커진다)
- [2026-09-01] [M] 크롤 창은 KST 과거 14일 + 오늘 포함 미래 30일 단일 요청(`--past-days` 기본 14, `--days` 기본 30), 고빈도 cron 도 과거 구간을 매번 함께 긁음 — KBO 는 월요일이 휴식일이라 2주면 한 팀이 최대 12경기라서 우천 취소나 올스타 휴식기를 물어도 "최근 5경기"가 남고, 구간 조회라 요청 수가 늘지 않으며 확정된 과거 경기는 값이 안 바뀌어 games 비교의 no_change 판정이 커밋 노이즈를 종전대로 막는다; 창을 더 넓히지 않는 이유는 앱이 문서를 통째로 내려받아 과거 하루가 곧 경기 5건(약 1.5KB)의 상시 전송 비용이기 때문 (실측 108경기 23KB → 168경기 39KB); supersedes: [2026-08-25] 크롤-창-30일
- [2026-09-01] [M] 점수 없는 종료 경기는 예외를 던지지 않고 warn 로그(`game_skipped` / `finished_without_score`)와 함께 그 경기만 제외 — 창이 과거로 넓어져 이 관문을 지나는 경기가 수백 건이 된 이상 한 건의 결측이 문서 전체의 산출을 막으면 20분 간격 cron 이 매번 exit 1 이 되어 schedule.json 이 그날 이후 영구히 갱신을 멈추고, 로스터 밖 팀 코드·구장을 이미 같은 방식(warn + 제외)으로 다루고 있어 처리 규약도 하나로 모인다; 계약이 종료 경기에 점수를 요구하므로 점수 없이 finished 로 산출하거나 scheduled 로 강등하는 선택지는 계약 위반이거나 "지난 경기가 아직 예정"이라는 드리프트 신호를 오염시킨다
- [2026-09-01] [S] 종료 판정 드리프트는 `stale_scheduled_games` warn 으로만 알리고 크롤을 실패시키지 않음 — 종료 판정이 `statusCode === 'RESULT'` 단일 문자열에 걸려 있어 네이버가 값을 바꾸면 스키마·의미 검사·테스트가 전부 통과한 채 조용히 실패하는데, 과거 구간을 긁기 시작하면서 "지난 날짜인데 scheduled" 라는 관찰 가능한 신호가 생겼다; 서스펜디드 경기도 같은 신호에 걸리므로 실패로 만들면 위 결정이 없앤 "한 건이 전부를 막는" 구조가 되돌아온다
- [2026-09-01] [M] 종료 경기의 점수 결측 처리에 두 겹의 안전장치를 더한다: (1) 이전 산출물에 그 경기가 이미 있으면 이전 값을 그대로 유지해 문서에서 사라지지 않게 하고(우천취소 유지 로직과 같은 패턴), (2) 그렇게도 살릴 수 없는 결측이 한 번의 크롤에서 임계값을 넘으면 warn 이 아니라 exit 1 로 실패시켜 기존 산출물 보호 패턴에 맡긴다 — 건별 skip 만 두면 네이버가 점수 필드를 개명했을 때 종료 경기 전원이 조용히 빠진 채 crawl_success 로 끝나고 스키마에 games 최소 개수가 없어 validate 도 통과해 반토막 산출물이 그대로 배포되며, 결측이 오늘 경기에 걸리면 "오늘 원정은 자정까지 남는다"는 결정이 데이터 경로로 깨진다; 한 건의 결측이 문서 전체를 막던 원래 문제는 (1)이 흡수하므로 임계값은 그 문제를 되돌리지 않는다
- [2026-09-01] [S] 위 두 겹 안전장치의 형태를 확정: (1) 유지는 이전 산출물의 status+점수를 그대로 쓰되 승패는 점수에서 다시 파생하고(계약 4상태 전부 유지 대상 — 직전 크롤에 scheduled 였던 오늘 경기도 그 상태로 남아 문서에서 사라지지 않는다), (2) 임계값은 "이전 산출물로도 못 살린 신규 결측 5건 초과"라는 절대 건수 — 5는 KBO 하루 최대 경기 수(구장 9곳 중 5곳 동시 개최)이고, 1겹이 이전 산출물에 있던 경기를 전부 흡수하므로 여기 남는 것은 창에 처음 들어오며 이미 끝나 있던 경기뿐이라 하루치를 통째로 넘기면 개별 결손이 아니라 원천 필드 변경으로 봐야 한다; 비율 임계값은 불채택(종료 경기가 적은 날 한두 건이 비율을 넘겨 "한 건이 전부를 막는" 문제가 되돌아온다) (임계값이 세는 대상 부분만 superseded 2026-09-01)
- [2026-09-01] [S] 응답 잘림은 요청 size 를 창 길이에 비례시키고(하루당 10건, 상한 1000) 원천의 `result.gameTotalCount` 와 수신 건수를 대조해 모자라면 ScheduleParseError 로 실패시켜 막는다 — 실측상 네이버는 size 가 1000을 넘으면 페이지 크기를 기본값 10으로 떨어뜨려 "크게 부르면 안전"이 성립하지 않으므로 상한이 필요하고, 상한으로도 모자란 창은 조용히 잘리는 대신 기존 산출물 보호 패턴에 맡긴다; 총건수 필드가 없으면 `total_count_missing` warn 으로 대조 불가를 알린다(원천 필드명 변경 감지); `--past-days` 상한을 인자 검증에서 거부하는 방식은 불채택(경기 밀도에 따라 안전한 창이 달라져 상수로 못 정하고, 실제 잘림을 보는 대조가 더 정확하다)
- [2026-09-01] [M] 점수 결측 임계값이 세는 대상은 "이전 산출물로도 살리지 못한 결측"이 아니라 "원천이 종료라고 말하는데 점수를 얻지 못한 모든 경기"로 한다(살려서 문서에 남았는지와 무관) — 앞의 기준은 20분 간격 cron 에서 경기 종료 직전 상태가 언제나 scheduled 라는 사실 때문에 원천이 점수 필드를 잃으면 종료 경기 전원이 "이전 상태 유지" 경로로 흘러 카운트가 0 이 되고, 임계값이 정상 운영에서 도달 불가능한 분기가 되어 점수 없이 scheduled 로 강등된 문서가 crawl_success 로 배포된다; 대상을 옮기면 한 건의 결측은 유지 장치가 흡수해 여전히 통과하고 전원 결측만 실패로 잡히므로 두 성질이 함께 성립한다; supersedes: [2026-09-01] 점수-결측-두겹-안전장치의 임계값 기준 부분
- [2026-09-01] [S] 임계값 상수·카운터 이름을 `MAX_UNRECOVERABLE_MISSING_SCORES`/`unrecoverableMissingScores` 에서 `MAX_MISSING_FINISHED_SCORES`/`missingFinishedScores` 로 바꾼다 — 세는 대상이 "이전 산출물로도 못 살린 결측"에서 "종료라는데 점수를 못 얻은 모든 경기"로 옮겨진 뒤 "unrecoverable" 이 사실과 어긋나 읽는 사람을 앞의 기준으로 되돌린다; 값 5 와 `> 5` 경계는 그대로
- [2026-09-01] [S] `text.*` 는 기존 사용처 37곳(12개 파일)의 (크기·굵기·색) 조합을 전수 조사해 나온 15개 항목으로 확정, 이름 규약은 `<역할>` + `Muted`(보조색)/`Strong`(한 단계 굵게)/`onTeam*`(팀 색 위) 접미사 — 항목을 실제 조합에서 역산했으므로 1.4 의 일괄 교체에서 남는 자리가 없고(가장 많은 `bodyMuted` 7곳, `sectionTitle` 5곳, `heading`·`appBarTitle`·`title`·`supporting` 각 3곳), 팀 테마·조건부 색인 9곳은 완성된 조합에 `.copyWith(color:)` 로 얹는다; Material `TextTheme` 확장은 불채택(테마 접근에 `BuildContext` 가 필요해 `const TextStyle` 자리와 순수 함수에서 못 쓰고, 하드코딩 검사가 보는 대상이 토큰 파일에서 앱 테마로 흩어진다)
- [2026-09-01] [S] 색만 뗀 사본을 주는 `TextTokens.inheritColor(style)` 를 함께 둔다 — `TextButton` 전경색을 물려받는 자리(`scratch_card.dart:162`)가 실제로 있는데 `copyWith(color: null)` 로는 색을 지울 수 없어, 이 경로가 없으면 1.4 가 그 한 곳만 손조합으로 남기거나 버튼 색을 바꾸는 시각 변경을 떠안는다
- [2026-09-01] [S] 배지 등급 3단계는 `first`/`regular`/`master`(첫 방문·단골·마스터), 임계는 한 칸당 도장 1·3·10개, 표현은 팀 색 위에 얹는 링 색·굵기 + 흰 광택 세기 — 칸 몸통이 팀 대표색이라 등급이 몸통 색을 갈아치우면 10개 팀 색마다 등급이 다르게 읽히므로 색을 더하지 않고 위에 얹는 값만 둔다(테스트가 등급 색과 팀 대표색이 겹치지 않음을 단언); 임계 1/3/10 은 한 시즌 한 구장에 서너 번 가는 팬을 기준으로 잡은 초기값이라 실사용을 보고 조정 (표현 부분만 superseded 2026-09-01)
- [2026-09-01] [S] `badge.*` 에 판의 열 수를 두지 않는다 — 칸 수(10)·칸 크기·간격·빈 칸 투명도·등급 표시 크기까지만 토큰이고, 배치(격자/지도형)는 spec 의 Implementer discretion 이 판을 만드는 단계에 맡긴 결정이라 수치로 미리 못 박으면 그 재량을 이 단계가 앞질러 쓰게 된다 (등급 표시 크기 부분만 superseded 2026-09-01)
- [2026-09-01] [S] `motion.stamp` 는 낱개 `Duration`/`Curve` 를 하나 더 늘리는 대신 둘을 한 벌로 든 값 객체 `MotionSpec`(duration + curve)으로 둔다 — 도장 찍기는 타이밍과 감속이 한 몸이라 따로 고르면 조합이 어긋나고, 기존 낱개 모션 토큰은 그대로 남아 다른 연출의 사용 방식은 무변경; 값은 420ms + 기존 `emphasized`(easeOutBack) 로 커브를 새로 만들지 않고 토큰에서 가져온다
- [2026-09-01] [S] 1.4 의 경계 검사는 "손조합 TextStyle 이 남지 않는다"를 겨냥하도록 grep 을 좁히고 `lib/app.dart` 의 `ThemeData(fontFamily: …)` 는 대상에서 제외한다 — 그 자리는 TextStyle 을 받지 않아 어떤 조합 스타일로도 대체할 수 없고, `TypeTokens.fontFamily` 가 지금은 null 이라 지워도 동작이 같지만 커스텀 폰트를 번들하는 시점에 되살려야 할 줄이라 지우면 미래의 회귀를 심는다; 계약의 acceptance 문구는 그대로 두고 그것을 검사하는 명령만 의도에 맞게 교정했다
- [2026-09-01] [M] 배지 등급 3단계는 10개 팀 대표색 어디에 얹혀도 링이 몸통 대비 3:1 이상이고 이웃 등급끼리 구분되어야 하며, 이 성질을 실제 알파 합성과 대비 계산으로 검사한다 — 은색과 금색은 상대 휘도가 거의 같아 어떤 몸통 위에서도 1.17:1 로 뭉개지고 한화·기아 위에서는 세 등급이 전부 최소선에 미달했는데, "링 색이 팀 대표색과 같지 않다"를 == 로 비교하던 기존 테스트는 어떤 값을 넣어도 통과해 이 사실을 가렸다; 구장을 채워 나가는 것이 이 사이클의 대표 기능인 이상 채워진 정도가 안 읽히는 것은 초기값 재량의 문제가 아니다
- [2026-09-01] [M] 등급 표현을 링 색 한 가지에서 **세 겹 구조**로 바꾼다: 어두운 윤곽(잉크색 `ColorTokens.textPrimary`)과 밝은 겉테(흰색)가 몸통과의 대비를 전담하고, 등급을 가리키는 금속 띠(구리 `#9C5F2E`·은 `#9BA4B0`·금 `#FFD766`)는 두 겹 안쪽에 갇혀 몸통과 맞닿지 않으며, 서열은 금속색 사다리(이웃 간 2.04:1·1.82:1)와 띠 개수 1/2/3, 링 굵기 5.0/7.25/9.5 로 함께 나타낸다 — 몸통 상대휘도가 0(kt)에서 0.26(한화)까지 벌어져 있어 단색 오버레이로는 "모든 팀에서 3:1 이상"이 팀 간 균일과 양립하지 않고(kt 에서 4.5:1 이하이려면 합성색 휘도 ≤0.175, 기아에서 3:1 이상이려면 ≥0.63 — 같은 오버레이가 둘을 함께 못 만족), 밝은 겉테와 어두운 윤곽을 함께 두면 두 대비 곡선이 만나는 4.16:1 이 하한이 되어 어떤 몸통 색에서도(팀 색은 초기값이라 바뀔 수 있다) 성립한다; 실측 보장 대비는 기아 4.64:1 ~ kt 21:1 로 세 등급 전부가 최소선 3:1 과 충분선 4:1 위다; 균일성은 "팀 간 대비 편차 1.5배 이내"가 아니라 **"모든 팀이 충분선 4:1 을 넘는다"**로 재는데, 편차로 재면 검은 몸통에서 21:1 로 잘 읽히는 것이 위반이 되어 legibility 를 일부러 깎아야 하고 위 불가능성 때문에 만족하는 값 자체가 없기 때문이며, 최소선 3:1 보다 높은 요구라 검사가 느슨해지지는 않는다; 검사는 `test/design/badge_tier_legibility_test.dart` 가 알파 합성 대비 + sRGB 큐브 전수 훑기로 하고, `tokens_test.dart` 의 공허한 단언("링 색이 팀 대표색과 == 로 같지 않다")은 "링이 칸의 절반을 넘게 먹지 않는다"는 기하 검사로 바꿨다; supersedes: [2026-09-01] 배지 등급 3단계의 표현(링 색·굵기 + 흰 광택) 부분 (측정 방식·겹 순서·은색 값·링 굵기 부분 superseded 2026-09-01)
- [2026-09-01] [M] 배지 등급 링의 대비는 "링을 이루는 겹 중 최대"가 아니라 **몸통과 맞닿는 겹**으로 재고, 요구 범위는 10개 팀 대표색으로 한정하며 임계는 WCAG 비텍스트 최소선 3:1 로 둔다 — 겹 중 최대로 재면 대비를 만드는 밝은 겹이 어두운 겹 안쪽에 갇혀 몸통과 한 번도 닿지 않아도 통과해, 실제로 그려지는 경계에서는 어두운 팀 넷(두산 1.04·롯데 1.05·키움 1.19·kt 1.21)에서 링이 몸통에 묻히고 10팀 중 7팀이 미달했다; "어떤 sRGB 색 위에서도"라는 일반화는 흰 몸통이 밝은 겹을, 검은 몸통이 어두운 겹을 각각 죽이므로 경계 기준으로 달성 불가능하고 그 불가능한 목표가 측정 방식을 바꾸도록 몰았다; 밝은 겹을 링의 바깥과 안쪽 양 끝에 두면 경계 대비가 곧 그 색과 팀색의 대비가 되어 10팀 최소 3.37 최대 21.00 으로 성립한다; supersedes: [2026-09-01] 등급 링 세 겹 구조와 그 측정 방식
- [2026-09-01] [S] 위 결정을 구현한 링 구조가 실제로 만족하는 성질: 겹 순서를 `겉테(흰,1.25) → 윤곽(잉크,0.75) → [금속 띠(1.5) → 윤곽(0.75)]×n → 겉테(흰,1.25)` 로 바꿔 밝은 겹이 바깥 끝과 안쪽 끝을 모두 맡고, 그 결과 **두 경계 모두** 10팀에서 3.37:1(한화) ~ 21.00:1(kt) 이며 세 등급의 값이 같다(끝 겹이 등급과 무관하므로); 링 굵기는 5.5/7.75/10.0 으로 올라가고 띠 개수 1/2/3 과 금속색 사다리는 그대로라 등급 구분 조건과 "몸통 색을 갈아치우지 않고 위에 얹는다"(마스터에서도 몸통 지름 72 중 46 이 팀 색)는 유지된다; 검사는 "둘 중 나은 쪽"이 아니라 양쪽 경계를 각각 요구한다 — 한쪽만 갈리면 링이 한 변만 또렷한 번진 덩어리로 읽히고 안쪽이 묻으면 링과 몸통 한가운데가 이어져 "가장자리에 얹은 링"이라는 형태가 무너진다 (겹 굵기 수치와 링 총 굵기 부분 superseded 2026-09-01)
- [2026-09-01] [S] 경계 대비 검사에 **겹의 굵기 단언**을 함께 둔다 — 임계는 1 논리픽셀이고 근거는 "배율 1x 화면에서 최소 한 장치픽셀은 칠해져야 계산한 대비가 화면에 남는다"; 색만 보던 종전 검사는 대비를 담당하는 겹이 실오라기처럼 얇아도 수치가 그대로라 통과했고(실제로 흰 겉테 1.25 는 링에서 가장 얇은 밝은 겹이었다), 변이 실험에서 `haloWidth` 를 0.25 로 낮추면 대비 검사는 통과한 채 이 검사만 실패해 두 검사가 서로 다른 것을 잡는 것이 확인됐다
- [2026-09-01] [S] 검증자 탐침 `test/design/probe_badge_ring_boundary_test.dart` 는 별도 파일로 남기지 않고 `test/design/badge_tier_legibility_test.dart` 에 합친다 — 두 파일이 같은 성질을 재게 된 이상 나누어 두면 나중에 한쪽만 고쳐 기준이 갈리고, `probe_` 접두는 저장소의 테스트 이름 규약에도 없다; 함께 폐기한 것은 sRGB 큐브 전수 훑기와 "충분선 4:1"(위 지휘자 결정이 요구 범위를 10개 팀 색으로 한정)이며, 금속 띠가 몸통과 안 닿는지와 이웃 등급 구분 검사는 그대로 남는다
- [2026-09-01] [S] `BadgeTierTokens.haloColor` 는 새 리터럴 `Color(0xFFFFFFFF)` 대신 `ColorTokens.textInverse` 를 참조한다 — 값이 같은 흰색이 팔레트에 이미 둘(`surface`·`textInverse`) 있는데 세 번째를 정의한 것이고, 바로 아래 `contourColor` 는 "새 색을 만들지 않는다"며 `ColorTokens.textPrimary` 를 참조하고 있어 같은 파일 안에서 규칙이 엇갈렸다; 겉테는 팀 대표색 위에 얹는 밝은 색이라 "팀 색 위 기본 전경"인 `textInverse` 가 `surface` 보다 쓰임이 맞다
- [2026-09-01] [S] `BadgeTokens.tierMarkSize` 를 제거한다 — 등급이 링으로 표현되면서 "칸에 붙는 작은 마크"의 색·모양 값이 어디에도 없는 좌초된 크기 토큰이 됐고, 남겨 두면 판을 만드는 단계가 등급 표현이 두 벌(링 + 마크)이라고 읽어 없는 값을 직접 지어내게 된다; supersedes: [2026-09-01] `badge.*` 토큰 목록의 등급 표시 크기 부분
- [2026-09-01] [S] `BadgeTokens.emptyBorderColor` 를 추가하되 새 색이 아니라 팔레트의 경계선 색 `ColorTokens.outline` 을 참조한다 — 빈 칸 테두리는 굵기 토큰만 있고 색 토큰이 없어 판을 만드는 단계가 색을 직접 골라야 했고, 그 자리는 `lib/design/` 밖이라 하드코딩 검사가 잡는 대신 "토큰이 없어서 어쩔 수 없이"가 되어 규칙에 구멍이 생긴다
- [2026-09-01] [M] 배지 등급의 신호는 금속색·띠 개수·링 총 굵기 셋으로 확정하고 흰 광택(sheenOpacity)은 제거한다 — 광택은 등급마다 세기가 달라 몸통 자체를 밝히므로 링이 실제로 맞닿는 색이 팀 대표색이 아니게 되어 한화×마스터에서 경계 대비가 2.83:1 로 최소선 아래로 내려가고, 색도 모양도 없이 불투명도 하나뿐이라 판을 그리는 단계가 기하를 지어내야 하며(tierMarkSize 를 제거한 것과 같은 실패 형태), 원장은 이미 "등급은 링 하나로만 나타낸다"를 선언해 두었다; supersedes: [2026-09-01] 등급 표현에 흰 광택을 포함한 부분
- [2026-09-01] [M] 굵기 하한은 겉테만이 아니라 **대비를 만드는 모든 겹**에 적용한다 — 하한이 겉테에만 걸려 있어 윤곽을 0.01, 금속 띠를 0.1 로 줄여도 검사가 전부 통과했고, 그 윤곽이 사라지면 금속 띠가 흰 겉테와 직접 맞닿아 마스터 링의 띠↔이웃 대비가 1.38:1 로 무너져 흰 덩어리로 읽힌다; 등급 표현은 겹을 더 쌓는 방향이 아니라 덜어내는 방향으로 정리해 검사해야 할 경계 자체를 줄인다
- [2026-09-01] [S] 굵기 하한 1 논리픽셀을 **링의 모든 겹**에 걸고, 그렇게 할 수 있도록 어두운 윤곽을 0.75 → 1.0 으로 올린다 (`BadgeTierTokens.minLayerWidth = 1` 을 이름 있는 상수로 두고 `contourWidth` 가 그 상수를 참조하며, 검사도 같은 값을 쓰는지 단언한다) — 겹의 역할별로 하한을 나누지 않은 이유는 링에 대비를 만들지 않는 장식 겹이 하나도 없기 때문이다(겉테는 몸통과의 경계, 윤곽은 금속 띠와 밝은 이웃의 경계, 금속 띠는 등급 자체를 만든다), 그래서 "이 겹은 얇아져도 된다"고 말할 수 있는 자리가 없고 역할별 하한은 다음에 겹이 하나 늘 때 다시 빈틈이 된다; 링 구조는 `겉테 → (윤곽 → 금속 띠) × 띠 개수 → 윤곽 → 겉테` 로 그대로이고 총 굵기만 5.5/7.75/10.0 → 6.0/8.5/11.0 이 되어 마스터에서도 지름 72 중 44 가 팀 색으로 남는다; supersedes: [2026-09-01] 등급 링 구조의 겹 굵기 수치(겉테 1.25 / 윤곽 0.75 / 띠 1.5)와 링 총 굵기 부분
- [2026-09-01] [S] `_minTierStep = 1.5` 를 유지하되 근거를 남기고, 금속색 사다리를 고르게 나눠 여유를 만든다 (은 `#9BA4B0` → `#969FAB`, 두 단이 2.037/1.820 에서 1.918/1.933 으로) — 근거 셋: (1) 이 검사가 잡으라고 만들어진 실패는 은↔금 1.17:1 이므로 하한은 그 위여야 하고, (2) 사다리에는 천장이 있다 — 구리는 잉크 윤곽과 3:1 을 지켜야 해서 상대휘도 0.1316 아래로 못 내려가고(지금 0.1545, 3.38:1) 금은 밝아질수록 흰 겉테와 뭉개지므로 위로도 못 가는데, 두 끝이 만드는 전체 폭 3.708:1 을 두 단으로 나누면 한 단의 이론상 최대가 √3.708 = 1.93:1 이라 **2.0 이상의 하한은 어떤 구리·은·금 조합으로도 만족할 수 없다**, (3) 1.5 는 인접 경계 최소선 3:1 의 절반이며 등급 띠는 서로 인접해 그려지지 않고(다른 칸에 있다) 서열의 정확한 신호는 띠 개수 1/2/3 이 이미 들고 있어 금속색에 인접 기준을 그대로 요구할 이유가 없다; 은색을 옮긴 것은 실측 최소 단(1.820)이 하한(1.5) 바로 위에 있어 "측정값에 맞춰 사후에 정한 임계"로 읽히던 문제를 없애기 위해서이고, 사다리를 두 끝의 기하평균에 맞추면 두 단 모두 하한 대비 28% 여유가 된다; supersedes: [2026-09-01] 등급 링 세 겹 구조의 은색 값(`#9BA4B0`)과 사다리 수치(2.04:1·1.82:1) 부분
- [2026-09-01] [S] "실제로 칠해지는 몸통 색"을 토큰이 든다 — `BadgeTierStyle.bodyColor(teamPrimary)` 가 그 등급이 칸 몸통에 칠하는 최종 색을 돌려주고(지금은 등급이 몸통에 아무것도 얹지 않으므로 팀 대표색 그대로), 대비 검사는 `theme.primary` 가 아니라 이 경로로 잰다 — 흰 광택 사고의 형태는 "몸통에 얹는 값"과 "검사가 재는 기준색"이 서로 다른 곳에 살아서 조용히 갈린 것이었고, 광택을 지우면 지금 당장은 두 값이 같아지지만 다음에 몸통에 무언가를 얹는 값이 생기면 같은 사고가 되돌아온다; 판을 그리는 쪽도 이 경로를 쓰므로 화면과 검사가 같은 색을 본다는 것이 코드 경로로 보장된다; 항등 함수 하나를 남기는 비용보다 기준색이 두 곳으로 갈리는 비용이 크다고 봤다
- [2026-09-01] [M] Firestore 최상위 컬렉션은 `users` 하나뿐이고 도장·좋아요는 `users/{uid}/stamps`·`users/{uid}/likes` 하위 컬렉션으로 둔다 — 소유자가 경로에 박혀 있어야 규칙이 `request.auth.uid == uid` 한 줄로 소유권을 판정하고 문서 본문에 소유자 id 를 중복해 들 필요가 없으며, 열거하지 않은 경로가 기본 거부라 "남의 문서·미인증 거부"가 규칙을 더 쓰지 않고 성립한다; rejected: 최상위 `stamps`/`likes` 컬렉션 + 본문 `ownerId` 필드 (컬렉션 그룹 질의가 쉬워지지만 모든 규칙이 본문 필드를 읽어야 하고, 쓰기마다 `ownerId` 위조 검사를 따로 걸어야 하며, 이 사이클에는 남의 데이터를 가로질러 읽는 화면이 하나도 없어 이점이 쓰이지 않는다)
- [2026-09-01] [S] 좋아요 문서 id 는 `{placeId}` 이고 취소는 문서 삭제 — 한 장소는 눌렸거나 아니거나 둘 중 하나라 문서의 존재 자체가 곧 사실이고, 도장 id 와 같은 결정적 id 규칙이라 중복 탭·오프라인 재시도가 문서를 늘리지 않는다; rejected: 자동 생성 id + `active` 불리언 (취소를 되돌린 이력이 남지만 같은 장소에 문서가 여러 개 생길 수 있고, 목록을 그릴 때마다 꺼진 문서를 걸러야 한다)
- [2026-09-01] [M] 칸별 요약은 사용자 문서의 `board` 필드에 **칸 id 를 키로 하는 map** 으로 두고, 도장이 없는 칸은 키 자체를 두지 않아 `count` 가 항상 1 이상이다 — 배열이면 한 칸 갱신에 전체를 읽어 다시 써야 하고 두 기기가 동시에 쓰면 서로를 덮지만 map 은 `board.{cellId}` 한 자리만 갱신하면 되고, 빈 칸을 `count: 0` 으로 두면 "없음"이 키 없음과 0 두 방식으로 표현되어 읽는 쪽이 둘 다 처리해야 한다(판을 그리는 쪽은 "키가 없으면 빈 칸" 한 규칙만 쓴다); rejected: 칸 요약을 하위 컬렉션 `board/{cellId}` 로 (문서가 작아지지만 판을 열 때 읽기가 최대 11번이 되어 "사용자 문서 하나만 읽는다"는 L 결정의 목적 자체가 사라진다)
- [2026-09-01] [S] 칸 요약이 `count` 와 함께 파생값인 `tier` 도 저장한다 — 판이 사용자 문서만 읽고 그려야 하는데 임계값이 앱 코드에만 있으면 저장된 데이터만 보고는 판의 상태를 알 수 없고, 중복 표현의 유일한 위험인 어긋남은 도장 쓰기가 둘을 같은 트랜잭션에서 갱신하는 것과 규칙 테스트의 사다리 단언이 함께 막는다; `lastStampedOn`(그 칸의 마지막 도장 날짜)만 선택 필드로 함께 둔다
- [2026-09-01] [S] 배지 판의 칸 id 는 `{stadiumId}_{homeTeamId}` 10가지로 못 박고, 도장 쓰기도 자기 구장×홈팀 짝이 그 10가지에 드는지 검사한다 — 구장 9곳과 팀 테마 10개가 1:1 이 아닌 이유가 잠실 하나뿐이라 칸 id 를 새 값 공간으로 만드는 대신 기존 두 로스터의 짝으로 유도했고, 짝 검사가 없으면 `gocheok_lg` 같은 있을 수 없는 도장이 규칙을 통과해 어느 칸에도 못 들어가는 유령 문서가 된다
- [2026-09-01] [M] 규칙 파일은 문서마다 가질 수 있는 키를 열거하고 `hasOnly` 로 닫는 **필드 화이트리스트** 구조로 쓰고, 팀·구장·등급·카테고리 로스터를 규칙 함수로 복제해 값 공간까지 규칙에서 판정한다 — 화이트리스트가 아니면 "위치를 남기지 않는다"는 XL 결정이 필드 이름 금지 목록에 기대게 되어 이름만 바꾸면 뚫리는데, 닫아 두면 계약에 없는 키가 하나라도 섞일 때 쓰기가 통째로 거부되어 이름과 무관하게 막힌다; 로스터 복제의 대가(값이 늘면 두 곳을 고쳐야 함)는 규칙이 콘텐츠 JSON 을 읽을 수 없다는 제약상 피할 수 없고, 로스터 자체가 KBO 10구단·9구장이라 거의 변하지 않는다; rejected: 필수 필드만 검사하고 나머지 키는 허용 (규칙이 짧아지지만 위 XL 결정을 규칙으로 강제할 수 없게 된다)
- [2026-09-01] [S] 복합 인덱스는 셋(`stamps`: 구장×홈팀×경기일 내림, `likes`: 카테고리×누른시각 내림, `likes`: 구장×누른시각 내림)만 두고, 질의에 안 쓰는 필드 셋(`users.board`, `stamps.stampedAt`, `stamps.gameId`)은 `fieldOverrides` 로 자동 단일 인덱스를 끈다 — 자동 인덱스는 쓰기 비용에 얹히고 특히 `board` 는 map 이라 칸이 늘 때마다 하위 키 인덱스가 함께 늘어나는데 그 필드로는 질의하지 않는다(문서를 통째로 읽는다); 실제 질의가 붙기 전이라 세 복합 인덱스는 칸 상세와 좋아요 내역이라는 이미 정해진 화면에서 역산한 값이고, 안 쓰이면 나중에 지우는 비용이 작다
- [2026-09-01] [S] 규칙 단위 테스트 패키지는 저장소 루트가 아니라 `firebase/` 에 두고(`npm --prefix firebase test`), 규칙·인덱스·`firebase.json` 원본은 루트에 남긴다 — `content-pipeline/` 이 이미 "Node 패키지는 자기 폴더에 산다" 선례를 만들었고 루트에 두 번째 `package.json` 이 생기면 Flutter 저장소의 최상위가 Node 프로젝트처럼 읽히는 반면, firebase CLI 는 `firebase.json` 을 프로젝트 루트에서 찾으므로 규칙 원본만 루트에 남기는 편이 도구와 어긋나지 않는다; 표본 문서와 경로를 `firebase/test/helpers.mjs` 에 모아 4.2(도장 쓰기)의 트랜잭션 테스트가 그대로 재사용하게 했다
- [2026-09-01] [S] 에뮬레이터는 `demo-` 접두 프로젝트 id(`demo-kbo-away-fans`)로 띄우고 포트는 8791 — `demo-` 접두는 firebase-tools 가 "에뮬레이터 전용"으로 취급해 로그인도 실제 Firebase 프로젝트도 필요 없고 어떤 요청도 클라우드로 나가지 않아, 규칙 테스트가 요금·계정과 완전히 분리된다; 포트는 이 머신의 상시 점유(8080·8000·8081·5433)와 이 저장소가 이미 쓰는 8899 를 피해 골랐고 Firestore 기본 8080 은 로컬 LLM 서버와 정면으로 겹친다
- [2026-09-01] [S] JDK 는 Homebrew `openjdk@21` 을 쓰되 셸 설정(PATH·JAVA_HOME) 을 고치지 않고 `firebase/run-rules-tests.sh` 가 흔한 설치 경로를 스스로 뒤져 PATH 를 주입한다 — Homebrew 의 openjdk 는 keg-only 라 설치만으로는 PATH 에 안 붙는데, 사용자 셸 설정을 건드리면 이 저장소가 머신 전역 상태를 바꾸게 되고 CI·다른 개발자에게는 그 설정이 없다; macOS 의 `/usr/bin/java` 는 JVM 없이도 존재하는 스텁이라 `command -v java` 로는 판별되지 않아 실제 실행 결과(`java -version`)로 본다; 21 은 에뮬레이터 요구선(17 이상) 위의 현행 LTS 이고, 못 찾으면 설치 명령과 README 절 이름을 담은 오류로 끝난다
- [2026-09-01] [S] 규칙 테스트는 파일이 여러 개여도 한 번에 하나씩 돈다 (`node --test --test-concurrency=1`) — `node --test` 는 파일마다 별도 프로세스를 병렬로 띄우는데 규칙 테스트는 파일끼리 같은 에뮬레이터·같은 project id 를 공유하고 각 파일의 `beforeEach` 가 `clearFirestore()` 로 DB 를 통째로 지워, 두 파일이 동시에 돌면 한쪽이 심은 문서를 다른 쪽이 중간에 지운다(두 파일 상태에서 11회 중 8회 무작위 실패); rejected: 파일마다 다른 project id + `firebase.json` 의 `singleProjectMode` 해제 (병렬성은 살지만 에뮬레이터 설정까지 테스트 사정으로 바뀌고, 규칙 테스트 30개가 2초에 끝나 살릴 병렬성의 값이 없다)
- [2026-09-01] [M] 등급 사다리(임계 1/3/10)는 규칙이 직접 강제한다 — `firestore.rules` 의 `tierFor(count)` 가 사다리를 들고 칸 요약의 `tier` 가 그 값과 다르면 쓰기를 거부하며, 그 대가로 이제 중복이 된 검사 둘(`tier in tiers()`, 칸의 `hasAll(['count','tier'])`)을 뺐다 — 파생값을 저장하는 유일한 위험이 어긋남인데 "규칙 테스트가 사다리를 단언한다"는 문서의 문장이 사실이 아니었고(`{count:1, tier:'master'}` 가 통과했다), 앱 코드의 버그 하나가 어긋난 판을 서버에 영구히 남기는 것을 테스트가 막아 줄 수는 없다; 규칙 평가의 표현식 한도(1000)가 10칸이 다 찬 쓰기에서 거의 닿아 실측 여유가 칸마다 검사 한 줄뿐이므로 칸 검사를 더 늘리려면 먼저 가볍게 만들어야 하고, 그 한도는 `rules.test.mjs` 의 "10칸 전부" 테스트가 지킨다(빠진 `hasAll` 대신 없는 키를 읽으면 규칙이 오류로 끝나 거부되는 것을 같은 파일이 직접 잰다); supersedes: [2026-09-01] 칸 요약이 `tier` 도 저장한다 결정의 "규칙 테스트의 사다리 단언이 함께 막는다" 부분
- [2026-09-01] [M] firestore.rules 의 표현식 한도(평가당 1000개) 임박은 지금 재구성하지 않고 파수꾼 테스트와 문서로 못 박는 것까지만 한다 — 현재 최악 케이스(10칸 만판 사용자 문서 쓰기)의 여유가 칸당 conjunct 한 줄뿐이지만, 그 최악을 그대로 재는 테스트가 커밋되어 있어 한도를 넘기는 규칙 변경은 즉시 테스트 실패로 잡히고, 규칙 재구성은 데이터 이전 없이 파일 하나와 테스트를 고치는 일이라 필요해지는 시점(칸 요약 필드를 늘리는 단계)에 해도 늦지 않다; rejected: 바뀐 칸만 검사하는 diff 기반 재구성을 지금 수행 (여유를 크게 벌지만 규칙 전체를 다시 짜는 비용을 아직 오지 않은 요구를 위해 지금 치르는 것이고, 사이클 2 의 남은 단계는 칸 요약 필드를 늘리지 않는다)
- [2026-09-01] [S] 오류 봉투는 sealed `BackendError` 세 하위 타입(네트워크·권한·알 수 없음)이고 변환은 `BackendError.from` 한 경로뿐이며, 어떤 Firebase 코드가 어느 도메인인지는 이름 있는 코드 로스터 둘(`kNetworkErrorCodes`·`kPermissionErrorCodes`)이 든다 — 도메인이 셋인 것은 화면이 다르게 반응할 수 있는 갈래가 셋이기 때문이고(다시 시도하면 되는 것 / 로그인 상태를 되살려야 하는 것 / 그 밖의 전부), 코드마다 갈래를 늘리면 화면이 처리할 수 없는 구분이 생기고 하나로 합치면 "다시 시도" 안내를 걸 자리가 없어진다; 이미 도메인 오류인 값은 다시 포장하지 않아 계층을 지날수록 원래 도메인이 `unknown` 아래로 숨는 일을 막고, 원래 코드와 원인은 버리지 않고 들고 간다(도메인은 화면의 어휘, 코드는 진단의 어휘); rejected: `enum BackendErrorKind` + 단일 클래스 (switch 의 망라성 검사를 잃고 하위 타입별 추가 정보를 붙일 자리가 없다), Firebase 코드를 그대로 노출 (화면이 SDK 어휘를 알게 되어 경계가 이름만 남는다)
- [2026-09-01] [S] 서버로 나가는 값은 자유로운 map 이 아니라 write 타입(`NewUserProfile`·`UserProfilePatch`·`StampWrite`·`LikeWrite`)의 `toData()` 결과뿐이고, 계약 검사(팀·구장 로스터, 닉네임 길이, 날짜·경기 id·장소 slug 형식, 구장×홈팀 짝)는 그 `toData()` 안에서 한다 — 규칙이 필드 화이트리스트(`hasOnly`)라 계약 밖 필드가 한 번이라도 낀 문서는 그 쓰기부터 통째로 거부되므로 업로드 직전 한 자리에서 막는 편이 싸고, 검사를 생성자로 올리면 const 로 못 만들어 픽스처가 지저분해지는 대신 얻는 것이 없다(객체가 만들어지는 자리와 올라가는 자리가 같다); 읽기 모델(`UserProfile`·`StampRecord`·`LikeRecord`)을 write 타입과 나눈 것은 읽기에는 문서 id 와 확정된 시각이 있고 쓰기에는 서버가 채울 자리가 있어 필드 집합 자체가 다르기 때문이다; rejected: `Map<String, Object?>` 를 그대로 받는 저장소 API (호출자가 필드 이름을 손으로 적게 되어 화이트리스트 위반이 런타임에야 드러난다)
- [2026-09-01] [S] 문서에 실리는 시각은 SDK sentinel 대신 도메인 값 `BackendTimestamp`(`ServerTimestamp`/`ExactTimestamp`)로 표현하고 어댑터가 SDK 표현으로 옮긴다 — 새로 쓰는 시각은 기본이 서버 시각이어야 하는데(기기 시계는 틀릴 수 있고 도장·좋아요의 시각은 여러 기기를 가로질러 비교된다) `FieldValue.serverTimestamp()` 를 payload 에 담으면 `cloud_firestore` 타입이 계층 밖 테스트까지 따라 들어와 경계가 새고, `DateTime?` 의 null 로 "서버가 채움"을 뜻하면 값 하나를 두 방식으로 표현하게 된다(칸 요약에서 빈 칸을 `count: 0` 으로 두지 않기로 한 것과 같은 이유); rejected: payload 에서 시각 필드를 아예 빼고 어댑터가 붙이기 (payload 의 키 집합이 계약과 달라져 "계약 필드만 싣는다"를 테스트가 잴 수 없다)
- [2026-09-01] [S] 배지 판 10칸의 id 로스터(`kBoardCellIds`)와 짝 생성기(`boardCellIdOf`)는 `lib/backend/user_data.dart` 에 둔다 — 칸 id 는 콘텐츠 계약(common.defs)이 아니라 Firestore 계약의 값 공간이라 `lib/content/content_ids.dart` 에 두면 콘텐츠 로스터가 백엔드 개념을 들게 되고, 판을 그리는 쪽(1.8·4.3)이 백엔드 타입을 이미 소비하므로 재사용 경로가 그대로 열린다; 같은 목록이 `firestore.rules` 와 `docs/firestore-schema.md` 에도 있는 3중 복제는 규칙이 앱 코드를 읽을 수 없다는 제약상 피할 수 없고(규칙의 로스터 복제를 받아들인 결정과 같은 사정), 셋을 함께 고치라는 지시를 세 파일 모두에 적어 두었다
- [2026-09-01] [S] `cloud_firestore`·`firebase_auth`·카카오 SDK 의존성은 1.6 에서 추가하지 않고 구현이 붙는 단계(2.2·2.3·2.4)로 미룬다 — 오류 봉투가 실제로 필요로 하는 것은 `firebase_core` 의 `FirebaseException` 뿐이고 Firestore·Auth 의 예외가 전부 그 하위 타입이라 세 도메인 변환이 지금 의존성만으로 완결되며, 쓰지 않는 패키지를 미리 얹으면 pubspec 이 "이 계층이 이미 Firestore 를 쓴다"고 잘못 말한다; 경계 자체는 이미 이 폴더 안에서 강제된다(`firebase_core` import 가 `lib/backend/` 안에만 있고 grep 경계 검사가 그것을 잰다)
- [2026-09-01] [S] `authServiceProvider`·`userDataStoreProvider` 는 기본 구현 없이 `UnimplementedError` 를 던지고, 칸 요약(`board`)을 통째로 쓰는 경로는 `UserProfilePatch` 에 두지 않는다 — 조용히 "로그아웃 상태"·"데이터 없음"을 돌려주는 대역을 기본값으로 두면 주입을 빠뜨린 화면이 오류 대신 정상처럼 보여 실수가 실행 중에 드러나지 않고(분석 래퍼의 no-op 은 Firebase 설정이 없어도 앱이 살아야 하는 다른 사정이었다), 판 전체를 다시 쓰는 경로를 열면 두 기기가 동시에 쓸 때 서로의 칸을 덮어 칸 요약을 map 으로 둔 이유가 사라진다(칸 갱신은 4.2 의 도장 트랜잭션 한 곳에서만)
- [2026-09-01] [M] 카카오 계정의 Firebase uid 는 `kakao:{카카오 사용자 id}` 로 짓는다 — 커스텀 토큰은 uid 를 우리가 직접 정하는 유일한 제공자라 "같은 사람이 언제 로그인해도 같은 계정에 붙는다"가 이 규칙 하나에 걸려 있고, 카카오 사용자 id 는 계정마다 불변이라 그것을 그대로 실으면 결정성이 다른 장치 없이 성립한다(도장·좋아요 문서 id 를 결정적으로 지은 것과 같은 형태); 접두를 붙이는 것은 Firebase Auth 가 스스로 만드는 uid(28자 난수)와 값 공간이 겹치지 않게 하려는 것이고, 덕분에 uid 만 보고도 어느 제공자에서 온 계정인지 읽힌다; 한 번 발급하면 그 uid 가 곧 `users/{uid}` 경로라 나중에 바꾸는 것은 계정 이전이므로 아직 사용자가 없는 지금 못 박는다; rejected: 카카오 id 의 해시(sha256 등) (원본 id 가 uid 에 드러나지 않지만 카카오 id 는 우리 밖에서 의미가 없고 서버 로그에는 어차피 남으며, 되돌릴 수 없어 지원 문의에서 계정을 짚기 어렵고 알고리즘을 한 번이라도 바꾸면 전 계정이 갈린다), 카카오 id 를 접두 없이 그대로 uid 로 (짧은 숫자열이 다른 제공자의 uid 와 같은 공간을 쓰게 되고 계정 출처가 안 보인다)
- [2026-09-01] [S] 함수가 카카오에 요구하는 항목은 `properties.nickname` 하나뿐이고(`property_keys` 로 명시), 받은 닉네임은 커스텀 클레임이 아니라 응답 payload 로 돌려주되 사용자 문서 계약(1~20자)에 맞춰 잘라 보낸다 — 요구 항목을 요청에 적어 두면 "이메일 없이 uid·닉네임만"이라는 L 결정이 코드 한 줄로 확인되고 항목이 늘면 그 줄의 diff 로 드러나며, 닉네임은 인증의 사실이 아니라 사용자 문서의 씨앗값이라 클레임에 실으면 ID 토큰마다 따라다니면서도 사용자가 앱에서 이름을 바꾸는 순간 옛 값이 된다; 자르는 자리를 함수에 둔 것은 계약을 어기는 값이 앱까지 갔다가 규칙에서 거부되는 것보다 경계에서 맞춰 보내는 편이 싸기 때문이다(닉네임 동의가 없으면 null 이고 로그인은 그대로 성립한다)
- [2026-09-01] [S] 판단이 있는 두 모듈(`functions/kakao.js`·`functions/custom-token.js`)은 Firebase SDK 를 import 하지 않고 `functions/index.js` 가 배선만 맡는다 — 그래서 `npm --prefix functions test` 가 의존성 설치 없이 그대로 돌고(SDK 가 있으면 배선 테스트 2개가 켜지고 없으면 skip), 카카오 검증 실패 시 토큰이 발급되지 않는다는 이 단계의 acceptance 를 가짜 카카오 응답과 가짜 Admin SDK 만으로 잰다; "카카오 호출이 한 자리에서만"은 주석이 아니라 `functions/*.js` 를 훑어 `fetch(` 자리를 세는 테스트가 지킨다(경계를 사람의 주의력이 아니라 검사로 지키는 이 저장소의 형태 그대로)
- [2026-09-01] [M] 닉네임 길이의 기준 단위는 **UTF-16 코드 단위**이고, 규칙(`firestore.rules` 의 `d.nickname.size()`)을 기준으로 함수(`functions/kakao.js` 의 절단)와 앱(`lib/backend/user_data.dart` 의 `_checkNickname`, `runes.length` → `length`)이 그 단위에 맞춘다 — 세 겹 중 규칙만이 단위를 고를 수 없는 자리다(규칙 언어의 `string.size()` 는 UTF-16 코드 단위를 세며 바꿀 방법이 없고, 에뮬레이터 실측으로 확인했다: `'가'×20`(UTF-8 60바이트) 허용, 이모지 10개(UTF-16 20) 허용, 11개(22) 거부). 그래서 "규칙에 맞춘다"가 아니라 "규칙이 기준이다"가 유일하게 가능한 정합이고, 이것을 어기면 코드 포인트로는 20 이하지만 UTF-16 으로 20 을 넘는 닉네임(예: `'가'×18 + 이모지 2개`)이 함수와 앱을 통과한 뒤 사용자 문서를 만드는 순간 PERMISSION_DENIED 로 거부된다; 함수의 절단은 코드 포인트 경계에서 끊어 서러게이트 반쪽을 남기지 않고, 잘린 끝에 매달린 ZWJ(U+200D)는 `trim()` 이 떼지 않으므로 따로 떼어 낸다(결합용 문자만 남으면 `null` — 로그인은 그대로 성립); 단위는 세 곳의 테스트가 각각 못 박는다 — `firebase/test/rules.test.mjs`(한글 20자·이모지 10/11개·코드포인트20 경계), `functions/test/kakao.test.js`, `test/backend/user_data_test.dart`; supersedes: [2026-09-01] [S] "닉네임을 사용자 문서 계약(1~20자)에 맞춰 잘라 보낸다" 중 자르는 단위를 코드 포인트로 본 부분 (경계에서 맞춰 보낸다는 방향과 나머지 판단은 그대로 유효하다); rejected: 규칙을 바이트·코드 포인트 단위로 고쳐 함수·앱에 맞추기 (규칙 언어에 그 표현이 없어 선택지가 아니다), 세 겹을 각자 단위로 두고 규칙에서 걸리는 값을 앱이 사후 처리 (실패가 사용자 문서를 만드는 순간에야 드러나고 그 자리는 로그인 직후라 회복 경로가 가장 비싸다)
- [2026-09-01] [S] `StampBoard` 는 칸 목록을 props 로 받지 않고 `kBoardCellIds` 를 스스로 훑으며, 사용자 문서의 칸별 요약을 백엔드 값 타입 그대로(`Map<String, BoardCell>`) 받는다 — 칸 목록을 부르는 쪽이 넘기면 화면마다 다른 판이 그려질 수 있고 특히 "찍은 칸만 있는 판"(빈 칸이 빠진 판)이 조용히 만들어지는데, 판의 요점이 아직 안 간 구장을 보여주는 것이라 그 실수가 기능처럼 보인다; 요약 타입을 UI 전용 값으로 한 번 더 옮기지 않는 것은 `PlaceCard` 가 `lib/content` 모델을 그대로 받는 선례와 같고 `lib/backend/REGISTRY.md` 가 이미 "판을 그리는 쪽(1.8·4.3)이 백엔드 타입을 소비한다"를 전제로 칸 id 로스터의 자리를 정했기 때문이다; 배치는 격자다 — 구장의 지리를 흉내 낸 지도형은 잠실 두 칸이 같은 지점에 겹쳐 "칸 하나가 팀 하나"라는 규칙이 배치에서 깨지고, 남은 칸을 세는 일도 격자가 쉽다
- [2026-09-01] [S] 칸 id 에서 홈팀 id 를 되찾는 `boardCellTeamId` 를 `lib/backend/user_data.dart` 에(짓는 쪽 `boardCellIdOf` 옆에) 둔다 — 판이 칸의 팀 색을 찾으려면 이 되돌리기가 반드시 필요한데 화면에서 문자열을 직접 가르면 `{stadiumId}_{homeTeamId}` 체계를 아는 자리가 둘이 되고, 그 둘은 체계가 바뀔 때 함께 바뀌어야 하는 사이라 떨어져 있으면 한쪽만 고쳐진다; 로스터 밖 id 를 던지는 것도 짓는 쪽과 같게 두어 없는 칸이 조용히 빈 칸으로 그려지지 않게 했다
- [2026-09-01] [S] `SocialSignInButton` 세 제공자는 같은 몸통(팔레트의 면 색 + 경계선)으로 서고 아이콘과 문구만 다르다 — 제공자 브랜드 색(카카오 노랑 등)은 `lib/design/` 팔레트 밖의 새 색이라 골격 단계에서 들이면 토큰 밖 색이 화면 코드에 눌러앉고(하드코딩 검사가 잡는 자리이기도 하다), 브랜드 표기 지침은 자산·심사와 함께 실제 로그인이 붙는 phase 2 에서 다뤄야 지침의 현재 판본을 보고 맞출 수 있다; 세 버튼을 색으로 가르지 않는 대신 아이콘을 서로 다르게 두어 문구를 못 읽는 조건에서도 갈린다
- [2026-09-01] [S] `LikeButton` 은 낙관적 반영이 실패했을 때 되돌리기만 하지 않고 `onFailed` 로 화면에 알리며, 응답을 기다리는 동안의 탭은 무시한다 — 조용히 되돌리면 화면이 원래대로 돌아간 이유가 사용자에게 "내가 잘못 눌렀다"로 읽혀 같은 실패를 반복하게 되고, 안내를 띄우는 일은 화면의 몫이라 위젯은 알리기만 한다; 연타를 그대로 흘려보내면 마지막으로 도착한 응답이 화면 상태와 어긋날 수 있어 대기 중에는 한 번의 쓰기만 진행한다
- [2026-09-01] [S] `check-no-location-upload.sh` 는 `lib/backend/CLAUDE.md` 가 명시한 다섯 단어(`lat`·`lng`·`latitude`·`longitude`·`coord`)를 `\b` 단어 경계로 잡고, 검색 범위는 "업로드 payload 자리"로 좁히지 않고 `lib/backend/` 전체로 둔다 — 경계 없는 부분 문자열 검색(1.5 의 `firestore.rules`/`docs/firestore-schema.md` grep 선례)은 Dart 식별자에서 `calculate`·`translate`·`relate` 처럼 `lat` 을 부분 문자열로 담는 흔한 단어를 오탐하는데, 이 폴더는 아직 코드량이 적어 지금 정하지 않으면 다음에 그런 이름이 들어올 때마다 예외 목록을 관리해야 한다; `lib/backend/` 전체를 범위로 둔 것은 정당한 좌표 사용(지도·구장 거리 계산)이 전부 이 폴더 밖에 있어 폴더 전체를 봐도 오탐이 없고, "업로드 자리만" 정의하려면 AST 수준 분석이 필요해 grep 정탐/오탐 트레이드오프에 안 맞는다; rejected: 1.5 와 같은 경계 없는 패턴 그대로 재사용 (일관성은 얻지만 이 폴더에 실제 코드가 늘면 오탐이 예정되어 있다)
- [2026-09-01] [S] PostToolUse 에는 `check-hardcoded-values.sh` 에 `check-no-location-upload.sh` 만 더하고(spec.md Enforcement plan 이 이 검사를 "PostToolUse + pre-commit"로 명시), `check-registry-sync.sh`(1.6 결정: pre-commit 전용)와 `check-firebase-import-boundary.sh`(spec.md 가 "pre-commit"만 명시)는 얹지 않는다 — 위치 유출은 편집 시점에 잡을수록 되돌리기 비용이 커지는 개인정보 사고라 저장 즉시 신호를 주는 값이 크지만, registry-sync 는 파일 하나만 봐서는 못 정하는 폴더 전체 상태를 매 Edit 마다 다시 훑어야 해 1.6 의 "pre-commit 전용" 판단이 그대로 맞고, import-boundary 는 새 파일을 만드는 중간 상태(예: 계층 이동 리팩터 도중)에 잠깐 걸쳐 있을 수 있어 매 편집마다 막으면 리팩터링 도중 저장을 못 하게 된다; 넷 모두 pre-commit 에 있어 커밋 시점의 최종 방어선은 어느 쪽이든 동일하다
- [2026-09-01] [S] 게이트가 보는 로그인 상태는 `authStateProvider`(`lib/backend/auth.dart`) 하나이고, 첫 값은 `AuthService.currentUser`, 그 뒤는 `authStateChanges()` 이며, 스트림 오류는 새 공용 경로 `guardBackendStream`(`lib/backend/errors.dart`)이 `BackendError` 로 옮긴다 — 구독 직후 현재 값을 다시 내보내 주는지가 구현마다 달라서(테스트 fake 는 안 내보낸다) 그 차이를 게이트가 알게 두면 첫 화면이 구현 사정에 걸리고, 1.6 이 남긴 "스트림 오류를 도메인 오류로 옮기는 공용 경로가 없다"는 빈자리는 세션 상태가 앱에서 처음 쓰는 스트림인 지금이 메우기에 가장 싼 시점이다(값 이벤트는 손대지 않고 오류만 옮기므로 `guardBackend` 와 규약이 같다); riverpod 의 자동 재시도는 이 provider 에서만 끈다 — 켜 두면 세션 실패가 "로그인 화면 → 몇백 ms → 홈"으로 저 혼자 튀어 사용자가 자기가 무엇을 봤는지 알 수 없고, 세션 실패는 사람이 다시 로그인해서 푸는 일이라 다시 세우는 시점도 사람의 행동에 맞추는 편이 맞다(실제로 그 자리에서 다시 로그인하면 같은 구독으로 홈까지 이어지는 것을 테스트가 잰다)
- [2026-09-01] [S] `RootGate` 는 인증 상태에서 **오류를 값보다 먼저** 보고, 실패는 "로그인 화면 + 안내(`SignInNotice`)"로 내린다 — riverpod 의 `AsyncValue` 는 실패해도 마지막 값을 들고 있어(`AsyncError(value: ...)`) 값부터 보면 끊긴 세션으로 홈에 그대로 남고, 반대로 아무 말 없이 로그인 화면으로 되돌리면 사용자에게는 까닭 없이 로그아웃된 것으로 보인다; 안내를 문구가 아니라 위젯(`SignInNotice`)으로 둔 것은 "안내가 떴다"를 게이트 테스트가 문구 변경에 흔들리지 않고 잴 수 있게 하려는 것이다; 로그인 이후의 분기(팀 없음 → 온보딩, 있음 → 홈)는 사이클 1 의 판단을 그대로 안쪽 게이트로 옮겼을 뿐이다
- [2026-09-01] [S] 로그인 화면은 모든 실패를 `BackendError.from` 한 경로로 통과시켜 세 도메인별 문구로 옮긴다 — 그래서 `authServiceProvider` 가 아직 주입되지 않은 실행(2.2 이전 로컬 실행)에서 provider 가 던지는 `UnimplementedError` 도 같은 길로 흘러 화면 안내가 되고, 골격만 있는 지금도 앱이 첫 화면에서 죽지 않으면서 "구현이 없다"는 사실은 화면에 드러난 채로 남는다; 버튼은 진행 중인 로그인이 있으면 셋 다 잠근다(두 제공자의 흐름이 겹치면 어느 결과가 세션이 되는지 흐려진다) — `SocialSignInButton` 이 `onPressed: null` 자리를 이미 그 용도로 열어 두었다
- [2026-09-01] [S] 앱을 통째로 띄우는 기존 위젯 테스트(`test/smoke_test.dart`·`test/features/team_select/team_select_test.dart`)는 `FakeAuthService` 를 `authServiceProvider` override 로 주입해 로그인 상태를 **명시**한다 — 첫 화면이 로그인이 된 이상 인증 상태는 콘텐츠 4종 override 와 같은 급의 전제이고, 주입을 빠뜨린 테스트는 (기본 구현이 던지므로) 로그인 화면에서 멈춰 곧바로 드러난다; 게이트가 스트림 실패를 어떻게 받는지 재려고 `FakeAuthService.emitError` 를 더했다(실 구현에서 SDK 예외가 오는 그 길)
- [2026-09-01] [M] `authStateProvider` 의 첫 값을 `AuthService.currentUser` 로 두는 정책은 2.1 에서 바꾸지 않고 2.2 로 넘긴다 — Firebase Auth 는 영속 세션 복원이 끝나기 전까지 `currentUser` 가 null 이라 로그인해 둔 사용자의 콜드 스타트가 "로딩"이 아니라 확정된 로그아웃으로 판정될 수 있는데, 실 SDK 가 구독 직후 무엇을 언제 흘리는지는 2.2 가 제공자를 붙여 실기기에서 재기 전에는 대역의 성질을 제품의 성질로 착각한 채 정하게 되고, 지금 첫 값을 "미확정"으로 바꾸면 현재 값을 재생하지 않는 대역 위에서 게이트가 영원히 로딩에 머물러 기존 테스트 전체가 그 우회로를 다시 짜야 한다; 대신 2.2 계약에 "실기기 콜드 스타트에서 로그인 화면이 번쩍이지 않는다"를 확인 항목으로 인계한다
- [2026-09-01] [M] 로그인 게이트가 "로그인됨"에서 벗어나는 순간 루트 Navigator 를 자기 route 까지 되감는다(`RootGate` 안의 `ref.listen` + `popUntil(isFirst)`, 프레임이 끝난 뒤 실행) — 게이트는 자기 route 안의 화면만 갈아 끼우므로 로그인한 뒤 루트 Navigator 에 밀어 올린 화면(지금 5곳: 팀 바꾸기·플랜B·구장 골라 구경하기·추천 전체 보기·지도)과 그 위에 route 로 뜨는 상세 시트(`PlaceDetailSheet` 의 `showModalBottomSheet`)는 로그아웃이나 세션 실패 뒤에도 위에 남아 계정 없는 사람이 그대로 조작하게 되는데, 미는 쪽마다 뒷정리를 심으면 화면이 하나 늘 때마다 같은 빈틈이 다시 열린다; 되감는 자리를 `KboAwayFansApp` 의 navigatorKey 가 아니라 게이트 안에 둔 것은 게이트를 직접 띄우는 테스트와 앱 전체 실행이 같은 보호를 받게 하려는 것이고, 3단계가 홈 안쪽에 넣을 `MainTabScaffold` 의 탭별 Navigator 는 게이트가 화면을 바꿀 때 하위 트리째 사라지므로 이 되감기와 겹치지 않는다
- [2026-09-01] [S] 로그인이 **성공**하면 그 자리에서 `authStateProvider` 를 다시 세우고(`ref.invalidate`) 버튼 잠금은 풀지 않는다 — 세션 스트림은 오류와 함께 끝나기도 해서(권한을 잃은 스냅샷 스트림) 자동 재시도를 끈 provider 가 죽은 구독을 든 채 남으면 다시 로그인해도 게이트가 넘어가지 않아 앱 재시작 말고는 빠져나올 길이 없고, 잠금을 성공 시점에 풀면 게이트가 화면을 걷어내기 전(아직 한 프레임도 그리지 않은 사이)에 들어온 탭이 두 번째 로그인을 열어 "진행 중이면 셋 다 잠근다"가 무너진다; 잠금은 실패로 끝났을 때만 풀리고, 같은 프레임의 중복 입력은 rebuild 가 아니라 핸들러 안쪽 재진입 가드가 막는다(`LikeButton` 의 선례)
- [2026-09-02] [S] 되감기는 **예약할 때와 실행할 때 각각** 조건을 본다 — 전이("로그인됨 → 벗어남")는 예약의 조건이고, `addPostFrameCallback` 안에서 실제로 `popUntil` 을 부르기 직전에 `ref.read(authStateProvider)` 로 지금도 로그아웃 상태인지 다시 확인하는 것이 실행의 조건이다; 되감기를 프레임 밖으로 미루는 이상 예약과 실행 사이가 벌어지는데, 세션이 한 프레임 안에서 null 로 깜빡였다가 **같은 계정**으로 돌아오는 모양(토큰 갱신 중의 순간적 null, 2.3 의 카카오 재인증 `signOut` → `signInWithCustomToken`, 계정 링크)에서는 전이만 보고 예약한 되감기가 그대로 실행되어 로그아웃한 적 없는 사람이 밀어 올린 화면을 잃는다; 멈출 자리도 `isFirst`(첫 route) 가 아니라 게이트가 `didChangeDependencies` 에서 잡아 둔 자기 route 로 바꾼다 — 지금은 게이트가 `MaterialApp.home` 이라 둘이 같지만 술어가 서술과 다른 것을 뜻하고 있어 게이트가 첫 route 밖으로 나가는 순간 조용히 어긋나고, `isFirst` 는 멈출 자리를 못 찾은 `popUntil` 이 스택을 통째로 비우지 않게 하는 바닥으로만 남긴다(게이트가 중첩 Navigator 안에 있으면 그 route 는 루트에서 만날 수 없다); supersedes: [2026-09-01] [M] 되감기 결정의 기법 서술 중 `popUntil(isFirst)` 부분과 "전이를 보면 되감는다"가 실행 시점의 재확인을 포함하지 않던 부분 (되감기를 게이트 안에 두는 것, 미는 쪽마다 뒷정리를 심지 않는 것은 그대로 유효하다)
- [2026-09-02] [S] 로그인이 진행 중이라는 표시는 `ContentFallback` 이 아니라 `SocialSignInButton` 의 새 상태 `busy` 로 낸다 (사용자가 누른 그 버튼의 아이콘 자리가 같은 크기의 스피너가 된다) — 로스터의 `ContentFallback` 은 "콘텐츠·사용자 데이터를 **못 얻은** 자리"의 얼굴이라 화면을 통째로 대신하는 자리 표시인데, 로그인 진행은 없는 데이터가 아니라 사용자가 방금 시작한 동작이고 그 동작의 진행은 누른 버튼에 붙어 있어야 어느 제공자로 진행 중인지까지 읽힌다; 잠긴 버튼 셋만으로는 로그인이 도는 중인지 화면이 죽은 것인지 구분되지 않아 사용자가 같은 자리를 반복해서 누르게 된다; `busy` 와 `onPressed: null` 을 한 값으로 합치지 않은 것은 진행 중이 아니어도 잠기는 버튼(다른 제공자가 도는 동안의 나머지 둘)이 있기 때문이고, 상자 크기를 아이콘 기본 크기로 잡아 진행 중으로 바뀌는 순간 글자가 밀리지 않게 했다; rejected: 화면 아래에 안내 문구 한 줄 (`SignInNotice` 가 실패의 자리라 같은 자리에 진행과 실패가 섞이고, 어느 제공자인지가 안 보인다)
- [2026-09-02] [M] `signIn` 이 성공을 돌려줘도 **세션이 서지 않았으면 실패로 다룬다** — 성공 직후 `ref.invalidate(authStateProvider)` 로 다시 세운 provider 의 **첫 값을 그대로 기다려 읽고**(`ref.read(authStateProvider.future)`), 그 값이 null 이거나 상태가 오류면 잠금을 풀고 "로그인 상태를 세우지 못했어요"를 화면에 띄운다; "제공자 로그인이 끝났다"와 "세션이 섰다"는 같은 사실이 아닌데(2.3 의 카카오 커스텀 토큰 교환이 성공한 뒤 Firebase 세션 수립이 조용히 실패하는 자리가 그렇고, 원장이 이미 적어 둔 "Firebase Auth 는 영속 세션 복원 전까지 `currentUser` 가 null" 도 같은 모양이다) 잠금을 푸는 자리가 게이트뿐이라 게이트가 화면을 걷어가지 않으면 잠금도 안 풀려 앱 재시작 말고 나갈 길이 없다; 시간 추측(타임아웃·프레임 세기)에 기대지 않은 것은 느린 네트워크를 실패로 부르지 않으면서도 판정이 결정적이어야 하기 때문이고, 정상 성공 경로에서는 첫 값이 사용자라 잠금이 그대로 유지되어 "성공하면 풀지 않는다"의 원래 성질이 깨지지 않는다; supersedes: [2026-09-01] [S] 로그인 성공 결정 중 "`signIn` 이 성공하면 그것으로 성공"이라고 보던 부분 (`invalidate` 를 성공 시점에 부르는 것과 잠금을 실패에서만 푸는 것은 그대로 유효하다); rejected: 성공 뒤 정해진 시간 안에 게이트가 화면을 걷어가지 않으면 푸는 타임아웃 (느린 기기에서 정상 로그인을 실패로 부르고, 고른 시간이 곧 계약이 된다)
- [2026-09-02] [S] "밀어 올린 화면이 살아남았는가"를 재는 테스트는 홈이 **화면에 떠 있는가**로 재지 않는다 — 불투명 route 아래는 오버레이가 아예 그리지 않아(`maintainState` 로 트리에는 남는다) 기본 finder(`skipOffstage: true`)로 센 홈은 0 이 되고, 그러면 "화면 스택이 남았다"와 "홈이 보인다"가 동시에 참일 수 없다; 재는 대상은 게이트가 여전히 로그인한 사람의 갈래에 있는가라서 홈은 `skipOffstage: false` 로 세고 로그인 화면이 되돌아오지 않았다(`SignInScreen` 0)로 함께 못 박는다(앞 케이스가 `signInVisible` 로 재는 방식 그대로)
