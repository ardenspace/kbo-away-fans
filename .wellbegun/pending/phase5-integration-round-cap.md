# Pending decision: phase 5 통합이 라운드 상한에 닿았습니다 — 어떻게 닫을까요?

## Situation

phase 5 통합 검증이 세 라운드를 돌았고 **세 번 다 REJECT** 였습니다. 세 번 모두
서로 다른 갈래였지만 뿌리는 하나입니다 — **5.2 가 4.1 의 판정 신호를 "지금 어디
있는가"로 다시 읽는데, 그 신호가 갱신되지 않는 구간이 있다**는 것입니다. 매 라운드
지휘자가 탐침을 직접 재현해 사실을 확인했고, 매 라운드 수정이 실제로 그 갈래를
닫았습니다(변이로 확인).

- round 1: 도장을 받으면 4.2 의 게이트가 창이 닫힐 때까지 판정을 건너뛰어, 구장을
  떠난 사람에게 **옛 구장 이름**이 계속 떴다. → 답에 시각을 붙여 닫음.
- round 2: 그 시각이 **홈이 다시 빌드될 때만** 재어졌고, 권한이 앱 밖에서 바뀌어도
  답이 갱신되지 않았다. → 복귀 신호에 매고 만료 계기를 둘 두어 닫음.
- round 3: **자리를 그릴지**를 정하는 쪽에는 그 잣대가 아직 안 대어져 있다(아래).

wellrun 의 규칙은 라운드 상한이 3 이고, 상한에 닿으면 지휘자가 혼자 더 돌리지 않고
사용자에게 선택을 넘깁니다. 그래서 멈췄습니다.

**열린 결함 (지휘자가 직접 재현했습니다).**
깨지는 문장은 step 5.2 의 acceptance criterion 2 — "권한이 없으면 그 자리가
사라지거나 권한 안내로 바뀌고 홈의 나머지는 그대로다."

걸을 수 있는 길: LG 팬이 사직 원정 경기(18:00)에 가서 앱을 연다 → 도장이 찍히고
홈 상단에 "사직야구장 근처예요" → 경기 도중 OS 설정에서 위치 권한을 끄고 앱으로
돌아온다(19:30) → **홈 상단 위치 자리가 그대로 서 있다.** 권한 조회 횟수가 복귀
전후로 1 → 1 이라 앱은 권한이 꺼진 것을 알 길이 없습니다. 시간 창이 닫히면(경기
시작 +5시간) 스스로 풀리지만 그때까지 최대 다섯 시간입니다.

검증자가 덧붙인 사실 하나: iOS 의 "이번만 허용"은 코드가 `granted` 로 접는 값이고,
그 사람은 **설정에 들어가 끄는 의도적 행동 없이도** 배경↔포그라운드 왕복만으로 이
상태에 닿습니다.

재현: `test/features/home/phase5_integration_round3_probe_test.dart` 의 `Q1` 에서
`skip: true` 한 줄을 지우면 `Expected: false / Actual: <true>` 로 exit 1 입니다.
같은 파일의 `Q1-b`(창이 닫히면 스스로 닫힌다)와 `Q2`(도장 없는 대조군은 자리가
접힌다)가 원인이 4.2 의 게이트임을 양쪽에서 못 박습니다.

**닫는 길은 좁아 보입니다.** 5.2 는 같은 어긋남을 **구장 이름** 쪽에서는 이미
닫았습니다(마지막 시도가 판정까지 가지 못했으면 그 판정을 "지금"이라고 말하지
않는다). 빠진 것은 **자리를 그릴지** 정하는 쪽에 같은 잣대를 대는 것이고,
`noGameToday`·판정 없음 두 갈래에는 권한 재조회가 이미 붙어 있으므로 "판정까지 가지
못한 실행"을 그 갈래에 함께 넣으면 됩니다. 측위가 아니라 다이얼로그 없는 권한
상태 조회 하나라 4.2 의 GPS 절제를 되돌리지 않습니다.

함께 볼 만한 계약 밖 발견 하나(F1): 구장에 **그대로 서 있는** 사람도 도장을 받고
앱을 배경↔포그라운드로 한 번만 오가면 게이트가 닫혀 문구가 구장 이름에서 일반
문구로 내려갑니다(실측). 계약 위반은 아니고 표기는 재량 안이지만, 위 수정과 같은
자리를 건드립니다.

현재 저장소 상태: `flutter analyze` 무지적 / `flutter test` **872통과·2스킵**
(스킵 둘 중 하나가 위 `Q1`) / 규칙 69 / functions 46 / 파이프라인 61 / 훅 4종 exit 0.

## Options

1. **라운드를 한 번 더 준다** — 되돌리기 등급 S. 닫는 길이 좁게 지목되어 있고
   4.2 의 절제를 건드리지 않는다. 다만 세 라운드가 매번 같은 뿌리의 새 갈래를
   찾아냈으므로 네 번째가 또 나올 가능성이 남는다.
2. **열린 발견을 기록한 채 phase 5 를 수용한다** — 되돌리기 등급 M. 사이클은
   지금 닫히고, 이 결함은 Deferred 로 남아 다음 사이클에서 다뤄진다. 그때까지
   앱은 권한이 꺼진 뒤에도 최대 다섯 시간 동안 위치 자리를 세운다(좌표를 읽거나
   보내지는 않는다 — 자리가 서 있을 뿐이다).
3. **열린 발견을 새 단계 5.3 으로 떼어 plan.md 에 붙인다** — 되돌리기 등급 S.
   자기 계약과 등급을 가진 단계가 되어 통합과 분리되고, phase 5 통합은 지금
   수용된다. 실행은 이 사이클 안에서 계속된다.
4. **라운드를 정해진 횟수만큼 더 준다** — 몇 번인지 답에 적어 주십시오.

## Recommendation

**1번(한 라운드 더)**, 까닭은 닫는 길이 이미 좁게 지목되어 있고 4.2 의 절제를
되돌리지 않으면서 사람이 실제로 걸을 수 있는 거짓("권한을 껐는데 앱은 여전히
위치를 쓰는 것처럼 보인다")을 이 사이클 안에서 없앨 수 있기 때문입니다.

## How to answer

번호로 답해 주시거나(또는 직접 고르신 길을 적어 주시면) 그 결정을
`decisions.md` 에 기록하고 이 파일을 지운 뒤, 멈춘 자리에서 실행을 이어갑니다.
