{
  "name": "kbo-away-fans-functions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "카카오 액세스 토큰을 검증해 Firebase 커스텀 토큰을 발급하는 Cloud Function — 배포와 앱 연동은 step 2.3.",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test": "node --test",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  }
}
